# Changelog

---

## Unreleased

Added (2026-09-20 — Belay's first commit, from a cleaned snapshot of the private Atlas archive)

- **The platform was renamed from Atlas to Belay**, records included — 858 lines across 136 of 232 tracked files (`git grep -n -i "atlas" main -- . | wc -l` in the archive), by the owner's ruling (`docs/OwnerDecisions.md` Part 14b) and **against the session's recommendation**, which is recorded there with its reasons. Three limits were applied and recorded so the owner can overrule any of them: the archive repository keeps its real name; addresses were not renamed except three an operator must follow; and the one hash-sealed artifact kept its stored contents, because renaming its `author` field broke its integrity hash and the suite caught it. Five tracked file paths were renamed, including `framework/atlas.py` and `scripts/atlas.py`
- **This repository's recorded history begins here.** Ids, pull request numbers and Issue numbers dated earlier refer to the private archive and will not resolve. The provenance note at the top of `docs/HANDOFF.md` says so, and no old citation was rewritten to hide it
- **Independent passes ran on the snapshot before it was pushed anywhere**, each told to falsify rather than to review, because `docs/OwnerDecisions.md` Part 13a makes that the owner's own condition for publishing the findings register. Between them they found a personal first name in a document ADR-002 freezes, a case-sensitive rename miss in the `LICENSE` liability clause, 52 citations wrongly shifted inside immutable review reports — twice overriding a rule `CHANGELOG.md:47` had already written down — and false statements in the records written *about* the rename, including a test citation that asserted the opposite of what it was cited for. What they found is in `docs/OwnerDecisions.md` Parts 14b and 14e through 14i. **The session's own review of its own work found almost none of them**
- **Two findings are open rather than fixed**, and are to be opened as Issues: ADR-013 rests on a premise that inverts once this repository is public (Part 14h), and `scripts/review_due.py` mis-numbers findings in a clone that has no remote (Part 14g) **Added later the same day, before the repository was made public (pull request `hardening/pre-public-guards`):** `tests/test_publication_guard.py` and `tests/test_rulesets.py` are new; `.github/rulesets/` holds the two rulesets for `main` as tracked files; `scripts/public_settings.py` applies and reads back the GitHub settings; `.github/CODEOWNERS` makes the guard files the owner's alone; `AGENTS.md` gains "Working In A Public, Shared Repository". No framework code touched. Appended to this line rather than inserted, so that no `CHANGELOG.md` line citation moves.

---


Added (2026-09-19, night — the repository is made ready for a second contributor working through a different AI provider. No framework code touched):

- **`AGENTS.md`** gains "Any Agent, Any Provider" (synchronize before trusting anything remembered; shared knowledge lives in the repository; fork, claim an Issue, the pull request's CI run is the gate, an independent pass before "verified"; what only the owner may change; how to ask the owner; citation rules; secrets and brokers) and "Determinism and Provider Independence". Its hard-coded "Current Known Priority" was stale and is replaced by a pointer to `docs/HANDOFF.md` and the dashboard.
- **`CONTRIBUTING.md`, `SECURITY.md`, `.github/pull_request_template.md`, `.github/CODEOWNERS`, `.github/ISSUE_TEMPLATE/task.md`** are new, and `.gitignore` gains a credential-shaped safety net appended at its end (the file is cited by line, so nothing was inserted higher up). ADR-002's freeze names `constitution/`, `departments/`, `workflows/`, `Knowledge/` and the markdown under `strategies/`; none of these files is in that list.
- **`LICENSE`** changes from MIT to a proprietary notice in the name "The Belay project owners" — owner-selected, `docs/OwnerDecisions.md` Part 11c. No lawyer has read it.
- **`README.md`** rewritten for an outside reader. It still names every lifecycle stage, which `tests/test_governance_conformance.py` requires.
- **`docs/research/known-data-sources.md`** — a non-binding note of data sources, custom-built workarounds with replication paths, and unsolved gaps, learned in the owner's separate private project. Leads, not evidence; it selects nothing.
- **`docs/OwnerDecisions.md` Part 11**, **`docs/HANDOFF.md`** ("Highest Priority Next Task" gains a night block in front of the evening list), **`docs/OperatorChecklist.md`** (a new last item under `## Open`), **`CLAUDE.md`** (now points at `AGENTS.md` and requires synchronizing first).
- **Every line citation that these insertions moved was repointed in the same change**, by a script that shifts only citations pointing below each insertion point, and excludes `reports/review/`. **The script's first run was wrong and is recorded, not hidden:** it matched `Validation/README.md:N` as though it were the root `README.md:N` and shifted three citations that should not have moved; they were reverted and the pattern fixed. **Four independent fresh-context passes then ran before merge** (falsification of this change; git history and GitHub metadata; the tracked tree; an outside reader and licence-consistency pass). The falsification pass checked 21 citation edits and about 15 prose claims and found four defects in this change, all fixed here: a repointed citation landing on a blank line, a shorthand citation the script could not see, a README sentence that ignored ADR-004's demotion floor, and an off-by-one in three `AGENTS.md` citations that predated this change and that the shift had faithfully preserved. It also found two older defects this change does not fix: a misquotation of `docs/HANDOFF.md` in ADR-011's text and in a test comment, and a stale suite count in this file. The publication passes found no secret in any object on any ref; what they did find is in `docs/OperatorChecklist.md`. A Windows username, a session id, a workflow-script id and a cloud-routine id were redacted in place from eight tracked files, and words identifying a private individual were bracketed out of Part 11's quotations. **Later the same night the owner approved the merge (`3c84cc0`) and ruled that Belay is published as a fresh repository from a cleaned snapshot, with this one kept private as the archive — `docs/OwnerDecisions.md` Part 12.** **At close of night the owner answered Part 12b's open points (Part 13): the new repository is named Belay, is the day-to-day source, and carries the full findings subject to two clean independent passes; `docs/proposals/fresh-public-repository-brief.md` briefs the build.**

Added (2026-09-19, evening — eleven further owner selections recorded as `docs/OwnerDecisions.md` Part 10, and two research proposals attacked. No framework code touched):

- **`docs/OwnerDecisions.md` Part 10 (10a-10k)** records eleven selections made by the owner after the session's earlier close-out: landing the register (PR #9 as `2f44c19`, PR #11 as `3080848`, both verified via `gh pr list`); running an independent pass on the evidence-bar proposal while the owner was away (17 defects, 4 blockers, all applied at `8e3edb2`); closing PR #4 as superseded by #9; authorising the review skill's numbering fix as PR #12 (two independent passes, 7 defects then 4; CI green; not merged); ruling F-004's *shape* while leaving its numbers open; authorising F-006's narrow fix and ruling its wide question as a calendar proposal first, with gaps failing closed once a calendar exists; accepting three glance items as recommended; asking for and being refused an unattended merge by Claude Code's own permission control; and installing a local, untracked pre-push guard against direct pushes to `main`. Every commit sha, PR number and file state cited is independently verified in this session, not merely repeated from what was asked
- **Every prior statement in this document that F-004 or F-006 was flatly unruled is corrected in place, dated, original wording kept** — Immutable Law VII. `docs/OwnerDecisions.md:988` (Part 8), `:1099-1102` and `:1281` (Part 9a and its closing note), `:914-916` (Part 7's reserved question, now answered), and `docs/HANDOFF.md`'s F-004 and F-006 entries and its "Highest Priority Next Task" all gain a dated correction rather than a rewrite
- **`docs/proposals/sample-adequacy-definition.md`** — a research proposal for F-004's numbers: adequacy as a minimum observation count AND a minimum time span, both required, neither depending on the observed effect, with regime coverage recorded as an open gap Belay cannot yet measure. Copied from the author's draft, independently attacked once (15 defects, 5 blockers, all applied — recorded in the document's own Appendix B), not yet re-verified a second time. Its citations into `docs/HANDOFF.md` and `docs/OwnerDecisions.md`, valid at `origin/main` `3080848` when first drafted, are re-verified and repointed by content against this branch, marked as the committer's edits. It carries seven numbered questions for the owner, one to be asked at a time with a recommendation and reasons, alongside the seven already waiting in evidence-bar-per-rung.md (not a resolvable path on this branch, so named without backticks) on PR #10's branch
- **`docs/HANDOFF.md` gains an "Evening addendum (2026-09-19)"** under "Completed This Session", and its "Current Repository State" and "Highest Priority Next Task" sections each gain a dated correction paragraph standing in front of the existing one rather than replacing it — the pattern this file has used all day. The next-task order now puts the owner's merge of PR #12 and this pull request first, then names the exact order the next session should ask its outstanding questions in: the six publication items, then the evidence-bar's seven, then the sample-adequacy proposal's seven, then the SKIP-override question
- **`docs/OperatorChecklist.md`** — the "merge #9/#11, close #4" item is marked `## Done` with the verifying shas; a local pre-push guard's installation is recorded `## Done`; and five new or updated `## Open` items are added: the PR #12 → records-PR merge order, six decisions owed before the repository goes public (with one scan figure corrected on independent verification — a Windows username appears in **5** tracked places, not the 6 first reported, `git grep` output kept as the check), the branch ruleset to apply immediately after going public, the rulings owed (updated in place rather than duplicated), and nothing written into the file that should not be — no email address, no username, no trigger or session id repeated where they are not already
- **`CHANGELOG.md`** — this entry, and the citation-hazard mitigation it describes below
- **A citation table was built before any edit and used to repoint every citation that moved, rather than trusting line arithmetic.** Every tracked file outside `reports/review/` was scanned for a `path:line` or `path:line-line` into `docs/HANDOFF.md`, `docs/OwnerDecisions.md`, `docs/OperatorChecklist.md` or `CHANGELOG.md` before any of the four was edited; each was recorded with the first 60 characters of the line it targeted. After all edits, each citation was re-matched by that content and repointed where the line had moved. `docs/DECISIONS.md` and files under `framework/` were touched, if at all, only for a number-only repoint of a citation already inside them — never for new prose
- **A usage limit interrupted two agents mid-task during the evening session, and both were resumed or re-run** rather than left incomplete; nothing in Part 10 or in either research proposal rests on a pass that was not either completed or redone

Fixed (2026-09-19 — the review skill stops assigning its own finding numbers; F-025 closed. Owner-selected in chat the same day, two selections and nothing wider: correct the numbering instruction, and take F-025's hardcoded counts in the same file. No framework code touched):

- **The instruction that cost seven finding numbers is gone.** `.claude/skills/belay-review/SKILL.md` read "Number them continuously across reviews — if the last report ended at F-014, this one starts at F-015", which is numbering from the previous report's *headings* — the mechanism that made four of the 2026-08-21 review's nine findings rediscovery. It now reads, at `:285-305`, that **the first finding takes the number `python scripts/review_due.py` printed on its `first finding: F-NNN` line** and that the number is **never** derived from where a previous report's headings ended, nor from `docs/HANDOFF.md`, nor from the skill itself. The skill said one thing in its cadence section and the opposite in its output section; it now says one thing in both
- **The cadence section states what the gate actually prints, because "call the gate" was not enough on its own.** `.claude/skills/belay-review/SKILL.md:42-79` now gives both verdicts verbatim — `DUE`, exit 0, followed by ` first finding: F-NNN`, run the review and start there; `SKIP`, exit 1, **no first-finding line because no number is allocated**, do not run and report the line and the date it names. It also states the twelve-day threshold (`scripts/review_due.py:36`) and that the script fetches and then reads every `refs/remotes/origin/*` ref rather than the working tree, which is the property the whole gate rests on
- **F-025 is closed, by deleting two numbers rather than by correcting them.** The skill told every reviewer to read "`docs/DECISIONS.md` — the 13 ADRs", of "158 KB"; `grep -c "^## ADR-" docs/DECISIONS.md` returns **14** and the file is **190,559 bytes**, and ADR-015 is drafted on PR #10's branch, so any restated count was due to rot again on merge. `.claude/skills/belay-review/SKILL.md:90-96` now tells the reviewer that `grep -c` is how many there are and `grep -n` is the index, and not to state a count there at all. **This is the report's own proposal** (`reports/review/2026-09-04-review.md:404`) rather than a substitute for it, and it follows the precedent `docs/DocumentStandard.md:5-7` already set for a count written as 81 that was 100 by the time anyone checked
- **Two more rotted numbers in the same skill, found by reading the rest of it and fixed here.** `.claude/skills/belay-review/references/red-team-checklist.md:43-53` cited `.gitignore:39-42` for the record-sufficiency claim that sits at `.gitignore:46-49`. **That citation was wrong when it was written, not rotted** — the first draft of this entry claimed ADR-014 had moved the claim down, and an independent pass falsified it: `git show 079f43a:.gitignore`, the commit that created the checklist, already has the unbounded-growth argument at `:39-42` and the record claim at `:46-49`, and ADR-014 (`a19c24e`) only rewrote `:46-49` in place. A miscount repeated for six weeks; and its B1 entry opened "Ten rules exist", which has never been true of this repository — `strategies/` holds twelve documents and no rule. Both now tell the reviewer to look rather than to trust the sentence. **Checked and left alone:** "all 11 documents" in `constitution/` is correct, `ls constitution/*.md | wc -l` returns 11
- **Three tests, confirmed red against the unfixed skill before the fix landed.** `tests/test_session_handoff.py:287-445`. The two positive checks are the load-bearing half: the first asserts the skill's numbering paragraphs name `python scripts/review_due.py`, and the third asserts the skill says to stop when the gate answers neither DUE nor SKIP. The second, negative test now strips inline code spans before matching, runs a widened backward-reference pattern sentence by sentence rather than paragraph by paragraph so a stray negation elsewhere in the block can no longer exempt it, and refuses the "ended at F-NNN … starts at F-NNN" continuation shape outright, negation or not. **Stated plainly, because F-013 is what happens when it is not:** an independent verifier still got three rewordings past it — "carry on where the one before it stopped… open this report at F-015"; "the highest F-NNN already used anywhere… add one to it", the hand-scan heuristic itself; and the banned wording placed inside backticks, since code spans are exempt by design. A regex recognises a string, not a meaning, and none of the three tests reads an instruction for sense
- **Open findings 42 → 41.** F-025's row and section leave `docs/HANDOFF.md`'s Open Findings, rows 39 to 42 renumber to 38 to 41, and the preamble carries a fifth "corrected again" paragraph rather than a rewrite of the four before it — Law VII. The closing entry states that the closure reaches `main` only when this pull request merges, since a reader on `main` today still finds "the 13 ADRs" in the skill. `docs/proposals/register-rebuild-session-brief.md`'s two bracketed count chains gain a third bracket each (42 → 41 now, and F-007's eventual close becomes 41 → 40), with every prior bracket kept
- **The unnumbered hazard note and the operator caution are corrected in place rather than deleted.** `docs/HANDOFF.md`'s Open Findings preamble carried the hazard with no finding number, and `docs/OperatorChecklist.md` carried "do not run a manual `/belay-review`" on the strength of it. Both keep their original paragraph — a reader who has only ever seen `main` still finds the old instruction there — and both gain a dated 2026-09-19 correction saying what the skill now says, where, and that it is true on this branch only. The checklist item **stays Open**, because the owner action it now asks for is to move it to `## Done` when this pull request merges
- **Citations into all six edited files were repointed by content, not by line arithmetic.** Every `path:line` into `docs/HANDOFF.md`, `CHANGELOG.md`, `docs/OperatorChecklist.md`, `docs/proposals/register-rebuild-session-brief.md`, `.claude/skills/belay-review/SKILL.md` and `.claude/skills/belay-review/references/red-team-checklist.md` in tracked files outside `reports/review/` was tabulated with the first 60 characters of its target line before any edit, and each was recomputed against that text afterwards. The repo's own citation test only checks that a cited line *exists*, so a stale target stays green — which is why this was done by content. Two of the repointed citations were **already** stale before this change by exactly the fifteen lines the cadence gate added on `a4e7a2a`: `docs/OwnerDecisions.md:1003` and `:1012` cite the severity table and the one-file-per-review rule, and both had been left pointing fifteen lines short. They now point at the text they describe. The one number deliberately **not** moved is `docs/HANDOFF.md:4222`'s quotation of what the 2026-08-21 review cited, which is a historical fact about that report rather than a live pointer

Fixed (2026-09-19 — session close-out, one commit, no framework code touched):

- **F-022 is closed.** `docs/HANDOFF.md`'s "Current Repository State" said the last session was 2026-08-02 and the suite was 626; both were re-derived and corrected — `main` at `cc80866`, PRs #6/#7/#8 merged 2026-09-19 (`a4e7a2a`, `b5e17d9`, `3af14a6`), suite **635 passed, 1 skipped, 5 xfailed**, **14 ADRs**, and which pull requests remain open and why. The 2026-08-02 text is not deleted — Law VII — it moves to a dated "State As Last Written, 2026-08-02" subsection kept in full. **What is not fixed, said plainly:** the report's proposed staleness check was not built, so nothing forces this section to be re-derived at the next close-out; that is left to whoever closes the next session, same as this one
- **Open findings 43 → 42.** F-022's row and section leave `docs/HANDOFF.md`'s Open Findings, rows 37 to 43 renumber to 36 to 42, and the preamble carries a fourth "corrected again" paragraph rather than a rewrite of the first three — Law VII. `docs/proposals/register-rebuild-session-brief.md`'s two forward-looking bracketed notes about a future "43 → 42" (written for whichever session closes F-007) are corrected in place to "42 → 41", with the original bracket kept and a newer one added rather than edited, since the register they describe has moved under them
- **`docs/HANDOFF.md`'s "Highest Priority Next Task" gets the actual next order**, added above the existing list rather than replacing it: the owner merges #9 then retargets and merges #11; an independent pass on the evidence-bar proposal (`docs/proposals/evidence-bar-per-rung.md`, on PR #10's branch only) precedes the owner's ruling on it; ADR-015's per-rung table is filled from that ruling, gets a fresh independent pass, and is ratified; only then are Phases 5-8 briefed for F-007 and F-014 together; only after that does the Part 9a queue continue with F-019, then F-003/F-008/F-016, then F-004/F-006 (still unruled — ask, do not assume), then F-024/F-027. Contains no name of a closed finding, checked against `tests/test_session_handoff.py:58-82` directly
- **`docs/OperatorChecklist.md` gains four Open items and one in-place update.** New: the merge order for PR #9, #11 and PR #4 (close as superseded, never delete); the rulings still owed (F-004, F-006 unruled; F-014's fix and ADR-015 unratified); three items worth an owner glance, drawn from this session's own findings about the register and the ADR-015 draft rather than from anywhere already on file; and a caution against running `/belay-review` manually while `.claude/skills/belay-review/SKILL.md:250-252` still numbers from the previous report's headings [**citation corrected 2026-09-19** — the skill was fixed later the same day and that instruction no longer exists; the numbering rule is now `.claude/skills/belay-review/SKILL.md:285-305` and says the opposite. Original wording left standing under Law VII]. Updated in place: the branch-protection item, with a fact not checked when it was added on 2026-08-27 — `gh api repos/.../rulesets` returns `403`, "Upgrade to GitHub Pro or make this repository public", which may or may not also gate the web picker the item's steps use
- **`docs/HANDOFF.md` gains "Completed This Session (2026-09-19)"**, and the three headings below it demote one notch each, the pattern this file has used before: "Completed This Session (2026-08-01)" becomes "Completed In The Previous Session (2026-08-01)", and "Completed In The Previous Session (2026-07-31)" becomes "Completed Previously (2026-07-31)"

Added (2026-09-19 — the two 2026-09 reviews are in the register, and two of their findings were already fixed):

- **F-019 to F-030 are rows and sections in `docs/HANDOFF.md`'s Open Findings, and the dashboard counts 43 where it counted 33.** The arithmetic is 33 + 10: twelve findings registered, **ten open and two closed on arrival**. `docs/OwnerDecisions.md:1120` (Part 9b, owner-selected 2026-09-19) is the authority, and it approves **registration only** — no fix for any of the twelve is authorised by it, none has a ruled fix, and a row here is a statement that the repository has read a finding rather than a decision about it
- **The two that were already fixed are registered as closed, with the closing evidence, rather than as live defects.** F-021 — five 2026-08-21 findings in no register the dashboard counts — is closed by `f6fb971` and `9ff5a58`, the two commits of PR #9 that this branch is stacked on; the closure reaches `main` when #9 merges, which is stated in the entry rather than glossed. F-026 — the cadence gate firing at seven days — is closed by `a4e7a2a` and `cc80866`, verified by running `python scripts/review_due.py`, which prints SKIP and exits 1. **Registering a finding that is already fixed is not a formality**: an unregistered closure is how a repository forgets that it checked
- **F-019 was re-derived by execution rather than carried over.** All eight one-token mutations were re-applied one at a time to a `git archive` copy of this branch, `__pycache__` cleared before every run, baseline confirmed `3 failed, 632 passed, 1 skipped, 5 xfailed` before the first and after the last — a `git archive` tree has no `.git`, so the three `git check-ignore` tests fail there regardless (`CHANGELOG.md:83`), which is not this branch's own `635 passed, 1 skipped, 5 xfailed`. **All eight still survive at that archive baseline.** The working tree was not modified. F-023, F-024, F-027 and F-028 were each reproduced the same way, by constructing the failure
- **Four citations carried over from the reports had moved, and each is repointed with the old line named.** `.github/workflows/conformance.yml:15-17` → `:17-19` and `:110-115` → `:112-117`, both moved by the numbering correction on `9ff5a58`; `.claude/skills/belay-review/SKILL.md:51-52` → `:66-67`, moved by the cadence-gate section landing above them [**corrected again 2026-09-19** — closing F-025 rewrote those two lines; what is there now is `.claude/skills/belay-review/SKILL.md:90-96`, and it states no count at all. Original wording left standing under Law VII]. Every other `path:line` in the twelve entries was re-read against this branch and is unmoved. **Nothing under `reports/review/` is edited** — one file per review, forever, and a report that was true when written stays as written
- **F-022 is registered open and deliberately not fixed here.** `docs/HANDOFF.md`'s "Current Repository State" still says the last session was 2026-08-02 and the suite is 626; it is 635. `docs/OwnerDecisions.md:1207` (Part 9d) rules that it is corrected at session close-out, so that this commit stays reviewable as one thing
- **One live hazard is recorded with no finding number, on purpose.** `.claude/skills/belay-review/SKILL.md:250-252` still tells a reviewer to number findings on from the previous report's last heading, which `CHANGELOG.md:64` identifies as the collision mechanism; `CHANGELOG.md:62` is where it cost seven already-claimed numbers on 2026-08-21 — and the same file now contradicts itself, because `:44-45` and `:51-53` were corrected when the gate landed [**corrected 2026-09-19** — the contradiction is resolved: the instruction at `:250-252` is gone, the numbering rule is `.claude/skills/belay-review/SKILL.md:285-305`, and the cadence section it used to contradict is `:42-79`. Original wording left standing under Law VII]. **Only `scripts/review_due.py` assigns numbers, at the next due review**, so this is a note in the Open Findings preamble for whoever runs it rather than an F-number minted by a session that has no authority to mint one
- **The table stays in registration order and the rows were appended, not sorted.** A P2 row now sits at position 34 below sixteen P3s, which is F-013 happening again in the commit that registers ten more rows — `scripts/status.py:213` sorts before the assertion compares, so `test_findings_are_ordered_by_priority` stayed green throughout. Recorded rather than hidden, as F-013's own section already says

Merged (2026-09-19 — three weeks of stalled work landed, and the cadence gate closed on both sides):

- **`main` had not moved since 2026-08-29.** Four pull requests were open and stacked: #6 (the cadence gate, not a draft, tested, +237/−0, open 21 days), #7 and #8 (the 2026-09-04 and 2026-09-11 review reports, both draft, report-only), and #4 (blocked, based on a review branch rather than `main`). All three mergeable ones were purely additive and merged clean, in order, each after its own five CI checks were confirmed green: `a4e7a2a`, `b5e17d9`, `3af14a6`. Suite 632 → 635
- **The stall was the bug, not a symptom of one.** The fortnightly gate listed `reports/review/` in the working tree, so the two unmerged reports were invisible to it and it kept reading a three-to-four-week-old report as the newest. It fired on a wrong answer four times — 2026-08-21, 2026-09-04, 2026-09-11, 2026-09-18 — and the first of those collided seven finding numbers that were already claimed and already ruled on, making four of its nine findings rediscovery. Merging #7 and #8 removes the blindness the gate was suffering from; merging #6 removes the gate's dependence on the working tree
- **Both halves of the gate are now closed, and they were never both in this repository.** `scripts/review_due.py` is the repository half. The account half was prose in the stored prompt of cloud routine `trig_<id>`, whose Step 1 now runs the script and obeys its exit code instead of listing a directory. `docs/OperatorChecklist.md` records both, and records that its own claim — that no session could edit that routine — was false and why the evidence for it was bad
- **Two further defects in that stored prompt, both read out of the 2026-09-18 run log rather than inferred.** It instructed `pip install -e .` on a dependency failure, which installs the runtime and no test runner; the run followed it, got `No module named pytest`, and retried with the `[dev]` extra. And it instructed the reviewer to continue finding numbers "from where the most recent report left off", which is the collision mechanism itself — headings are what one pass called its findings, not the durable series
- **The 2026-09-18 firing is worth recording as a near miss rather than a success.** It did not write a fourth duplicate review, but it did not stop at the gate either: it proceeded past Step 1 on the wrong answer, went looking at remote refs unprompted, ran the then-unmerged `review_due.py` out of a temporary directory, and only then skipped. Four firings in a row were saved by a reviewer noticing, which is the shape of control this repository keeps mistaking for a control
- **#4 was repointed from `review/2026-08-21` to `main` and is now `CONFLICTING`, correctly.** It registers nine findings as F-007..F-015 under the 2026-08-21 titles, but `main` assigns F-007..F-013 to the 2026-08-14 findings and `447f6a4` remapped the 2026-08-21 set to F-014..F-018 with four withdrawn as rediscovery. Rebasing it would reinstate withdrawn findings under numbers that now denote other findings. **It needs a rebuild to five rows, not a rebase** — which is F-021, already open and already saying so
- **Nothing was verified by a real firing.** The next scheduled run is 2026-09-25; the gate returns SKIP until 2026-09-23. Until that run completes, the account half of this change is untested in the environment that uses it

Added (2026-08-26 — the first thing in this repository that verifies anything without a human choosing to run it):

- **`.github/workflows/conformance.yml`. Until it landed there was no CI of any kind** — no `.github/`, no tracked config for any CI system, nothing that ran on a push or a pull request. `AGENTS.md:7` states "GitHub is the canonical source of truth", and the canonical source verified nothing: every green figure in this file, in `docs/HANDOFF.md` and in every review report was produced by whoever wrote the change, on their own machine, and reported by them. That is the self-certification `docs/HANDOFF.md:5416-5418` records the cost of — four sessions running, a fresh-context pass found 4, 6, 6 and then 19 defect-grade errors in work whose author's own review found none
- **Three jobs, and `governance` is split out from `suite` on purpose despite being redundant with it.** `tests/test_governance_conformance.py` is the enforcement arm of the governance layer — 1,166 lines that parse `constitution/` and hold the code to it — and nothing has ever guaranteed it runs. A breach of the constitution should not arrive looking like an ordinary unit test failure; the value of the second job is the name of the check that goes red
- **`dashboard` runs `scripts/status.py`, which is a real gate rather than a duplicate.** It exits non-zero when the suite is not green, and fails outright if the regular expressions that parse prose stop matching. `docs/DocumentStandard.md` is still "specified, not applied", so the dashboard reads headings, priority lines and ADR status blocks with regexes, and each one is a silent failure waiting for somebody to rewrite a sentence
- **`requires-python = ">=3.11"` was an untested claim and is now established.** `pyproject.toml:10` asserted it for the life of the project against evidence from exactly one interpreter. Checked before the workflow was committed, by building a clean venv per version and running the suite in each: **3.11, 3.12 and 3.13 each return 626 passed, 1 skipped, 5 xfailed.** The matrix is therefore a regression guard rather than an open question. If a version later goes red the fix is to narrow `requires-python`, never to drop the version from the matrix
- **`actions/checkout` and not an export, for a reason worth recording.** Three tests shell out to `git check-ignore` to prove that what is meant to be outside version control is, and that what is meant to be tracked is — `tests/data/test_store.py:206`, `tests/data/test_dolt_clone.py:598`, `tests/artifacts/test_repository.py:366`. All three fail without a `.git` directory: a `git archive` tree returns 3 failed, 623 passed, and `git init` in the same tree restores 626
- **`pip install -e ".[dev]"`, not `pip install -e .`.** pytest is an optional extra at `pyproject.toml:26`, so the bare install produces a working runtime and no test runner. That cost the 2026-08-21 review session two commands to discover and is now written where it cannot be forgotten
- **What this does not do, stated because a green badge invites the opposite reading.** It catches mechanical drift only. The 2026-08-21 review found two places where a one-token mutation to load-bearing logic passes all 626 tests — F-013's `is_clean` and F-014's missing `abs()` [**numbering corrected 2026-09-19 — read F-016 and F-017.** This bullet was written 2026-08-27 using the 2026-08-21 report's own heading numbers. Under the authoritative correction block at that report's head, `is_clean` is F-016 and the missing `abs()` is F-017; durable F-013 is the priority-order tautology and durable F-014 is the promotion recorded with no evidence. Original wording left standing under Law VII] — and four more defects no test could see at all. It also cannot reach the data path: the suite touches no network and spawns no subprocess by design, so `scripts/verify_clone.py` remains the only check that exercises a real fetch and it needs `dolt` and a 730 MB clone no runner has. **A green badge here means the suite ran, which is a weaker claim than health and is the only claim this workflow is entitled to make**
- **Branch protection is not in this change and is the half that makes it a gate.** Requiring these checks on `main`, and requiring a pull request, is a repository setting rather than a file — and it changes who may write to the canonical source, so it is an owner ruling and belongs in `docs/OwnerDecisions.md` rather than being applied silently. Until it is set, this workflow reports and does not block

Fixed (2026-08-02 — F-002 closed, ADR-014 implemented in all nine rules):

- **`artifacts/RPT-0001/1.0.0.yaml` is the first artifact of any type this repository has ever committed.** It is a signed fetch record for `data/market/dolthub-stocks/AAPL/0001-a7dbcaf3fb6aba2d.bin`, and `git ls-files` no longer returns nothing. F-002 said `fetch_and_record` built the record ADR-013 rule 3 requires, returned it, and nothing ever wrote one — so `.gitignore`'s stated reason for not committing the bytes, that "the record of what was obtained is signed and permanent", was false from the day it was written
- **The AAPL bytes were adopted, not back-dated.** ADR-014 rule 8 forbids a retroactive record: the original fetch timestamp is unrecoverable and a record is signed over `created`, so inventing one would file an Assumption under Facts. The window `2024-01-02..2024-02-09` was read out of the stored payload's own `sql_query` fields rather than guessed, re-fetched, and came back **byte-identical** — so `store()` returned the existing version 1 and the new record's `content_hash` proves it describes the file that was already there. Orphans 1 → 0, and nothing was asserted that was not observed. The first attempt failed with three `context deadline exceeded`s and the second succeeded, which is finding 14's recorded intermittency unchanged
- **The identifier space needed no governance amendment, which was the thing to establish before anything was written.** `\d{4}` capped every prefix at 10,000 identifiers for all time; `Knowledge/Identifiers.md` states no digit count and gives seven examples that all happen to use four, and `framework/identifiers.py` had turned the examples into a rule. Widened to `[0-9]{4}|[1-9][0-9]{4,}` — an alternation rather than `[0-9]{4,}`, which would admit `RPT-00217` beside `RPT-0217` as two directories for one number, permanently. `identifier_key` added for the reason `version_key` exists: the width is no longer fixed, so `RPT-10000` sorts before `RPT-9999` as a string
- **Rule 6 landed before rule 4, which was the only ordering available.** The record gains the source key, the store version and the store-relative path; it carried `source.name` alone — the display name `contract.py` says is *expected* to be edited, while the store is keyed on `source.key`, which never changes — so joining a record to its bytes meant hashing the whole store. The path is POSIX and store-relative because it sits inside the integrity hash: a backslash would make the identical fetch sign differently on two machines. ADR-013 rule 7 established that changing signed content is free only while nothing is stored, and rule 4 closed that window
- **`ARTIFACT_ROOT = artifacts/`, tracked, a module constant for the reason `STORE_ROOT` is one.** `ArtifactRepository.__init__` took `root` with no default, so every caller chose one and no two had to agree — which is why "where do artifacts live" had no answer in code or in any of the nine `Knowledge/` documents read for one, and why `.gitignore` could not name a directory in the claim it was making about them
- **`orphan_versions()` answers "which stored bytes have no record".** Matched on the store path *and* the hash computed from the file, never on either alone. `SeriesStore.all_versions()` walks every depth rather than answering per-symbol, because a per-symbol check can only cover symbols somebody thought to ask about — the shape of the gap that let F-002 run for the life of the module

Fixed (found by an independent verification pass over the above — **93 claims checked, 23 wrong, 19 defect-grade, of which the author's own review found none**, the fourth consecutive session with that shape):

- **The pass ran after implementation and ADR-014 requires it before.** `docs/HANDOFF.md`'s START HERE, written by the same author in the same session, placed it after step 6; the ADR's Required Follow-Up says before. Two governance documents, one mandatory step, opposite orderings, and the implementer followed the weaker one — by which time rule 6's one-way door had closed. Recorded in the ADR's Status block rather than quietly reordered
- **The worst defect reintroduced the failure rule 6 was written to make impossible.** Rule 5's dedup matched on `content_hash` alone, so two symbols whose payloads hashed identically collapsed into one record: the second fetch's bytes were written to disk, the record handed back named the *first* symbol and the first path, the caller's identifier was silently discarded, and `orphan_versions` then reported the store **clean** because it matched on the same hash. Now keyed on `(store_path, content_hash)`, which is exact
- **Second: the dedup returned `latest()` rather than the version that describes the bytes.** `records_by_content_hash` threw the version away, and `get(identifier)` with no version returns the newest — while `An Identifier Reused At A New Version Is Silently Mis-Filed`, already open, makes an identifier holding two versions reachable through sanctioned calls. It now returns `(identifier, version)` and asks for that exact one
- **Third: the widening meant to stop two directories denoting one number left three more ways to do it.** `\d` is Unicode-aware, so `RPT-` followed by Arabic-Indic or Devanagari digits validated and produced the *same* `identifier_key` as `RPT-0123`; `$` matches before a trailing newline, so a newline-terminated identifier was a fourth. Found one commit after the fix it undermines. Now `[0-9]` and `\Z`
- **Fourth: `all_versions()` walked exactly two directory levels.** `glob("*/*")`, so a version file directly under a source, or one nested deeper, was invisible — and `orphan_versions` returned `()`, which reads as "clean" rather than as "I did not look". The same shape as the per-symbol gap the method exists to close, one level up
- **Fifth: the record's hash guard compared two values the caller supplied and never opened the file.** A record naming a store path that held nothing was accepted and signed — rule 6's headline enforced for the hash and unenforced for the three fields the rule actually added
- **Sixth: `test_the_default_artifact_root_is_tracked_by_git` asserted `returncode != 0`**, which passes when `git check-ignore` returns 128 because it could not run at all. The store's own test asserts `== 0` and fails correctly in that situation. This is the tautology shape this repository keeps shipping
- **Seventh: `verify_clone.py`'s rule 3 guard reported PASS while pointed at the tracked root.** It compared an absolute path to `Path("artifacts")`, which is never equal, and asked only about `.parents`
- **Eighth: one unparseable YAML under `artifacts/` halted every fetch in Belay** with a bare `TypeError`. It now raises naming the file, and deliberately does not skip: a silent skip would make a half-written record read as an *absent* one, and rule 5 treats absent as "these bytes are new", so a corrupted record would mint a duplicate instead of raising an alarm
- **Every one of the eight has a regression test, and each was confirmed red against the pre-fix implementation** by stashing the source and re-running
- **And the ADR's own measurements were wrong.** "A fetch record is 2,732 bytes" reproduces only with a whole-second timestamp — `utc_now()` carries microseconds — and the draft then projected growth for the *post*-rule-6 world using a *pre*-rule-6 number, while rule 6 sits in the same document and adds three fields. "1,535 bytes of survivorship constant" was the Python string length rather than its serialised size. Recomputed against the record that actually shipped, 2,869 bytes: the tracked record is **3.84×** the ignored payload (3.7 in the draft), **64.3%** of the AAPL file (61%), and growth is ~36 MB/year at 50 symbols and ~4.3 GB at 6,000. Four citations were also wrong — `store.py:36-39`, `store.py:89-96`, and `contract.py:199-206` twice — and are repointed and marked inline
- **Four findings opened, none fixed:** ADR-014 rules 5 and 8 contradict each other as written, and only a lookup the ADR never names tells them apart; rule 7's orphan check is vacuous on every machine but the one that fetched, which is rule 9's own gap inside rule 9's implementation; `store.py:49` still carries the four-digit cap the identifiers shed, so the 10,000th restatement of a symbol writes a file the store cannot see and version numbering restarts at 1; and every fetch reads every stored record, which makes a universe backfill O(n²)

Ruled (2026-08-02 — ADR-014 accepted, implemented in no part):

- **ADR-014 answers F-002: where artifacts live, what git tracks, and what a `.gitignore` may claim.** F-002 is not a missing `save()` call — no document in `Knowledge/` or `docs/` names a directory for artifacts, and `ArtifactRepository(root)` has always made every caller pick one, which is why nine `Knowledge/` files plus `docs/Architecture.md` and `AGENTS.md` were read for a storage location and none has one. **So rule 1 is labelled a choice, not a derivation:** `artifacts/` at the repository root, as a module constant, the way `STORE_ROOT` already names the data store. Rule 2 tracks everything under it, on three differences from the bytes that are properties of the things rather than preferences — and the middle one runs *opposite* to ADR-013 rule 4: the bytes are ignored partly because they can be re-obtained, and **a fetch cannot be re-observed**. Rule 4 makes `fetch_and_record` persist, on the argument its own docstring already makes for storage — "these are one act, and separating them is how the failure goes silent" — which is F-002 with the halves swapped. Rule 5 emits one record per stored *version* rather than per fetch, because `store.py` already ruled the parallel case. Rule 8 writes **no retroactive record** for the one series Belay holds: its fetch timestamp is unrecoverable and a record is signed over `created`, so inventing one files an Assumption under Facts; a re-fetch either adopts the bytes by hash or leaves them beside a version 2 that has a record. Rule 9 permits a `.gitignore` comment to assert another subsystem's property **only where something checkable holds it**
- **Four things measured rather than argued, because rules 2, 5 and 9 turn on magnitudes.** A fetch record is **2,732 bytes** of YAML, of which **1,535 is the survivorship constant** — identical in every record that source will produce, and undeduplicatable because ADR-013 rule 5 exists so the caveat travels *on* the artifact. The one series Belay holds is **4,462 bytes**. The payload envelope costs **633 bytes per monthly chunk** and rows about **114 bytes** each. **So at the daily cadence the owner chose, the tracked record is 3.7× the ignored payload** (~747 bytes against 2,732), while for a thirteen-year backfill the ratio inverts completely. Both are true at once, which is why `.gitignore:46-49`'s "nothing is lost by this" cannot be repaired by rewording — it compares the wrong two quantities, and rule 9 refuses it in any form
- **The growth this accepts is stated with a trigger rather than a limit.** ~34 MB/year of permanently tracked records at 50 symbols on a daily cadence; ~4 GB/year at 6,000. The first is survivable for years and the second is not. No retention policy is ruled — Law VII points away from deletion and no document sets a limit, the same ground on which ADR-013 declined one for the store — so **the trigger to revisit is the one ADR-013 already named for buying data: the first time Belay selects instruments rather than being handed them**
- **Three findings opened by the drafting, none fixed, and the pattern is now three for three** (ADR-010, ADR-013, ADR-014). All three are facts about the repository as committed rather than consequences of the ruling. **The fetch record cannot name the bytes it records:** it carries `source.name` while the store is keyed on `source.key`, and `contract.py:199-206` says in terms that the name "carries the licence attribution CC BY-SA requires" and is expected to be edited — so the one source identifier the record carries is the one the store deliberately does not use, leaving the content hash as the only join, by brute force. **The identifier space cannot fund one record per fetch:** four digits means 10,000 `RPT` identifiers ever, permanent and never reused, so a 6,000-symbol backfill spends 60% of them in one run and nothing allocates them — and the four-digit limit is `framework/identifiers.py:33-35`'s, not `Knowledge/Identifiers.md`'s, which nobody had noticed. **An identifier reused at a new version is silently mis-filed:** same id and differing content at the same version raises correctly, but bumping the version is *accepted*, and two unrelated fetches become two versions of one artifact with `latest()` naming one current and `get()` vouching for it — every check passes because each record is individually sound. Found by running it
- **Ratification changes no code and the ADR says so in its Status block.** `.gitignore:46-49` still carries the false claim, `fetch_and_record` still returns a record nobody stores, and the AAPL bytes still have none. **Rules 6 and 4 must land in that order** — rule 6 changes signed content, which ADR-013 rule 7 established is free only while nothing is stored — and **rule 4 is blocked on the identifier ruling**, because identifiers are permanent

Fixed (2026-08-02 — two findings from the 2026-08-01 review, F-002/F-003/F-004/F-006 deliberately untouched):

- **F-001 is closed: a strategy can no longer be born already standing at PRODUCTION.** `ArtifactFactory.create()` now refuses any `strategy_stage` other than `IDEA` on a STRATEGY artifact. **The guard was on movement and not on birth**, which is the whole finding: `validate_transition` refuses `IDEA -> Micro Capital`, but the factory passed `strategy_stage` straight through `**kwargs` and `ArtifactValidator._validate_lifecycle` checked only that it *was* a `StrategyLifecycle`, never *which* one — so a signed STRATEGY artifact at `PRODUCTION` validated `True` carrying no VALIDATION artifact and no PAPER_TRADING history, because it had never occupied those stages. Breach: `constitution/Paper_First_Capital_Doctrine.md:5`, "Every strategy begins with zero capital. No exceptions." **The refusal is unconditional** — `grep -rn "ArtifactType.STRATEGY" framework scripts departments` returns only two lines in the validator, so no production code creates one and there was nothing to migrate — and it is shaped like the `integrity_hash` and `evidence_level` refusals directly above it, resting on the same sentence: **a caller-provided stage is an assertion nobody checked**. The factory is the right chokepoint rather than the validator, because `serializer.load()` builds `Artifact(**values)` directly and `repository.get()` verifies the hash on read, so a hand-edited YAML at PRODUCTION already failed while a factory-minted one passed everything. **Two existing tests were creating strategies past IDEA and both were fixed rather than exempted:** `test_deliverables.py` used PAPER_TRADING where the stage was incidental, and `test_serializer_round_trip.py` needed a strategy genuinely at LIMITED_CAPITAL and now builds one the way one actually comes to exist — born at IDEA, then `dataclasses.replace` and re-signed. **That the earlier test asked the factory for LIMITED_CAPITAL directly is the finding in miniature**
- **F-005 is closed: `DailyBarSeries` no longer accepts the same trading day twice.** The ordering guard was `dates != sorted(dates)`, and `[d1, d1, d1, d2]` is sorted, so a series could carry four observations over two trading days and present itself as validated. Now strictly increasing, which subsumes the ordering check it replaces. **It ran in the flattering direction:** a duplicated bar yields a fabricated `0.0` return in `period_returns`, and fabricated zeros pull the standard deviation down while barely moving the mean — the standard error falls, the t-statistic rises, and `significant` flips true on a record that did not earn it, with the fetch record's `observations` count and every artifact's `provenance` string inheriting the inflated count and nothing anywhere reporting it. **Closed as a prediction rather than an observation:** the one landed AAPL series has no duplicates and the current chunker does not overlap, so this never produced a wrong number; the route in was a vendor emitting a repeated row, or any future adapter concatenating overlapping windows. Breach: `constitution/Operational_Constraints.md:5`
- **Both were run against the unchanged code first, and both failed there.** Ten tests added — `test_a_strategy_cannot_be_born_past_idea` parametrized over all eight non-`IDEA` stages including RETIRED, `test_a_strategy_may_be_born_at_idea` for the other half, and `test_bars_must_not_repeat_a_trading_day`. Against the pre-fix source the nine refusal cases all reported `DID NOT RAISE ValueError`, which is the defect stated as a test result. Suite 584 → 594; open findings 19 → 17

Added:

- **Finding 14 is closed: Belay can complete a multi-year backfill.** `framework/data/dolt_clone.py` implements a second `Callable[[str], bytes]` transport behind the seam `DoltHubStocksSource` already exposed, so `MarketDataSource` did not change and neither did the parsing, the chunking or the survivorship answer. **Three years of AAPL — 754 daily bars — returns in 4.4 seconds** against roughly 25 minutes and frequent failures over the hosted API; 13 years drops from an estimated 6.6 hours per symbol to about 20 seconds. Cost: the `dolt` CLI 2.2.3 and a **730 MB** `--depth 1` clone at `data/upstream/`, gitignored **before the first byte landed**, and tested by asking `git check-ignore` itself rather than reading the file for a string. `--depth 1` is what keeps it under a gigabyte — Belay needs historical *prices*, which are rows, not historical *commits*; the consequence is that Dolt's `diff` cannot show how a vendor restated a price, which nothing needs today. Suite 551 → 584
- **A fifth wrong reading of the transport ceiling, from the run that closed it.** `docs/HANDOFF.md`'s table records "8 days → 0.4s"; the equivalent **novel** window measured **37.4s**, and a one-month aggregate **52.8s** against the server's ~54s deadline. The fast figures are warm-cache readings, which the finding already suspected and a cold start confirmed. The corrected comparison table carries **only one row measured as the same statement on both sides** — 11.9s over HTTP against 0.078–0.135s locally — because an independent pass refused a table whose every row compared different queries
- **What gets hashed on the clone path is a ruling, and it is written into the code rather than assumed.** The clone-backed source takes **its own store key**, so two transports' version chains never interleave — sharing one would make a route change register as a vendor restatement, silently, in the only mechanism Belay has for detecting a real one. Because the keys differ, **byte-identity with the HTTP envelope is not required and is deliberately not attempted**: forging it means reproducing DoltHub's exact key order and whitespace, and one space off diverges anyway while the author believes it matches. **The clone's commit hash is excluded from the payload**, since `dolt pull` would otherwise re-hash every series whose rows had not moved. **Stated cost, which the first draft recorded only as a benefit:** at the switchover every HTTP-fetched series becomes an orphaned chain, and a restatement straddling the switch is invisible
- **The clone is faithful to the source, established by query rather than asserted.** The hosted API and the clone were asked for the identical window and returned identical values including the awkward ones — `2017-10-26` high `157.8295`, volume `17000469`. `scripts/verify_clone.py --cross-check` performs that comparison live, after an earlier version of the check compared the clone against two constants typed into the script under a label claiming it matched the API

Fixed (found by an independent verification pass — **65 claims checked, 17 wrong or questionable, 6 defect-grade, of which the author's own pass found none**, the third consecutive session with that shape):

- **The worst removed a guarantee while claiming to preserve it.** `DoltCloneStocksSource` defined `survivorship` as a class-level property "to make the inheritance explicit". It was **inert** — the parent already returned that object — and it **defeated the second of the two links** defending ADR-013 rules 5 and 9. `fetch_record._survivorship_of` catches a subclass that skips `super().__init__()` by asking whether the validated constructor ever ran; a *class-level* property answers `getattr(source, "survivorship", None)` whether it ran or not. Demonstrated rather than argued: the guard **held** on the HTTP branch and was **defeated** on the clone branch. Deleted, and `test_a_subclass_that_skips_the_constructor_is_still_refused` now pins it — plus a second test asserting the attribute is inherited rather than restated at all, which fails for a different reason
- **A clone failure escaped the wrapper that names which chunk failed.** `dolthub.py` wraps a failing chunk because "a thirteen-year backfill is 156 requests and a message that does not say which month failed turns a one-line fix into a search" — and it caught `DoltHubUnavailable` only. **The transport that makes 156-chunk backfills routine is the one that lost the diagnostic.** Fixed with a shared `SourceUnavailable` base; the two concrete types stay siblings, because a deadline is worth retrying and a missing binary is not. The wrapper re-raises the **original type**, so naming the chunk cannot relabel a missing local clone as a hosted-API failure. **The docstring's stated reason for the separation was also wrong** — it claimed a shared type "would invite retrying a missing clone three times", but `_attempt` calls the transport *outside* its `try`, so a raising transport is never retried; measured at one call, not three
- **`_translate` was an unnamed third phantom-restatement door.** The hashed bytes on this path are partly Belay's own serialisation, so three cosmetically different implementations over identical vendor rows produced three different content hashes — `sort_keys=False` alone moved it — with nothing going red. Ruled: the envelope is a **frozen constant, and editing it is a migration rather than a refactor**, pinned byte-for-byte by a test and carrying an `_ENVELOPE_VERSION` so a deliberate change announces itself in the payload instead of masquerading as a restatement
- **`_DoltSql` had no test coverage at all**, and mutation testing showed what that cost: deleting the exit-code check left the suite **green**. That check is load-bearing — `dolt sql -q "SELECT 1; SELECT * FROM nope"` writes **valid JSON to stdout and exits 1**, so without it a truncated result is stamped `Success` and stored as complete, indistinguishable from a symbol that stopped trading. Replacing the argv list with `shell=True` also left the suite green, so the "a list, never a shell string" claim was a comment rather than a property. Five tests added; the source comment defending the *wrong* failure mode is corrected — plain text on stderr was never the hazard, because `_translate` refuses non-JSON loudly
- **The test fixture claimed to be dolt's output and was the other transport's.** It carried `17040200`/`21207300` copied from `tests/data/test_dolthub.py`, where the clone returns `17757230`/`21207098`, and quoted `volume` as a string where `dolt sql -r json` emits a **number**. The claim "`_bar` reads both, which was checked rather than hoped" was itself unchecked — the suite never saw a numeric volume. Fixture replaced with bytes copied from the live clone, and `test_a_numeric_volume_is_read_as_an_integer` added
- **The determinism test was a tautology that over-promised.** A fake runner returned one constant through a pure function, and its docstring claimed to defend "row order" — which `sort_keys` sorts nothing of. Kept for the injected-clock case it does catch, with the docstring corrected, and joined by a real envelope pin and a row-order test
- **Two smaller ones from the same pass:** an exit-0 object lacking `rows` became a clean empty success, so a hypothetical `{"error": ...}` would have arrived as zero bars — the back-door survivorship failure through the one path nobody checked, now refused; and the URL's branch segment was silently discarded while its query was honoured, so a request for another branch ran against whatever the clone had checked out

- Finding 15 — **a fetch stores its bytes before discovering the series is empty.** Pre-existing in `fetch_record.py` and made *routine* by the clone: `fetch_and_record()` stores the payload, then builds the record, and building it calls `series.period()`, which an empty series refuses. The bytes are already on disk in an append-only store. **Not an edge case on this source** — `delisted_prices=SURVIVORS_ONLY` means every dead ticker in a backfill hits it. **Deliberately not fixed**: an empty result is a *real answer*, and whether it deserves a fetch record is a ruling rather than a repair. Three candidate resolutions recorded, none chosen, because deciding it inside a transport change is the failure ADR-004 named. Surfaced by `scripts/verify_clone.py` on every run, labelled `KNOWN`

- **The implementation's independent verification pass found 6 defect-grade errors across 44 claims, and the author's own pass found none of them.** A worse ratio than ADR-013's drafting scorecard (4 defect-grade, author caught 1), on code whose tests were written first and were all green. **The worst undid the point of the session:** rule 5 requires the survivorship answer to be fixed "on every artifact derived from the source", and the implementation fixed the constant *inside* `SurvivorshipDisclosure` while letting the caller choose which disclosure object to pass beside a caller-supplied `source_name` **string** — so a fabricated `RETAINED / RETAINED, basis="trust me"` paired with the real source's name produced a signed artifact disclosing no bias at all. **The test named "a caller cannot substitute a kinder survivorship disclosure" asserted only that an unknown keyword raises `TypeError`**, which passes for any misspelling. Fixed by removing the parameter rather than validating it — the provenance functions take the source object, `survivorship` is read-only, and the check is repeated where the answer enters a signed artifact, because a subclass skipping `super().__init__()` bypassed the constructor entirely. **Second: rule 4 was wired to nothing** — `SeriesStore` had no caller outside its own tests, so every record carried a `content_hash` pointing at bytes nobody kept, which is rule 4's own argument for storing, describing the code. **Third: the IBKR adapter was non-deterministic** — `(date.today() - start).days` made the request a function of the day it ran, breaking rule 3's re-fetch comparison, and *the test written to catch it passed against the defect* because both calls ran on the same calendar day. Three more were documentation: a superseded measurement left in a comment as the stated basis for the chunk size, a test docstring claiming "eighty seconds per symbol" where the repo's own figure is 6.6 hours, and **ADR-013's Status block still reading "Not implemented" one commit after `0a6d5f7`, whose entire subject was closing stale ADR-013 statuses**
- **A failure mode this repository had not previously named: implementing a document invalidated its own citations.** ADR-013 rule 1 cited `framework/metrics/reporting.py:58` and rule 7 cited `:59`. Both were correct when written and were broken by inserting `SamplePeriod` above `Disclosure` — in obedience to rule 7. Repointed and marked rather than silently repaired
- **The source is intermittent, not merely slow, and that correction put a retry in.** Re-running a window that had already succeeded produced a failure at 54.6s followed by success in **1.0s on the next attempt**. An earlier note saying "retrying does not help" was true only of the three-month window (3/3) and false in general. ADR-013 rule 2 blesses retry explicitly — it is half of that rule's argument for two contracts, "retrying is *how* a data fetch recovers from a dropped connection; a retried order submit doubles a position" — so a bounded retry now covers the deadline failure and nothing else, because a bad branch does not become a good branch by asking again. **Finding 14 stays open**; this makes the current transport usable at the sizes it can already serve
- **The data store is keyed on a stable `key`, never on the display name.** Keying on `name` meant that adding the CC BY-SA attribution to it silently started a fresh version chain and made the existing history unreachable, with a re-fetch reporting version 1 and nothing saying anything had been lost — the silent irreproducibility rule 4 exists to prevent, introduced by rule 4's own implementation. Also from the same pass: `_slug` was not injective, so `DYN$A`, `DYN/A` and `DYN A` shared one version chain; the symbol regex used `$`, which admits a trailing newline; and three tests asserted tautologies over their own fixtures
- **ROADMAP Stage 2 is built. ADR-013 implemented 2026-08-01 in all ten rules**, and `framework/data/` exists where nothing did — the home ADR-002 requires, rather than an eleventh four-line placeholder in `framework/services/`. Suite 388 → 520, every test written first and run against unchanged code. **The sentence that was true of every number Belay had ever computed — that the series was supplied by hand and describes no real instrument — is false by construction now:** `disclosure_from()` derives a metric's `data_source` and `sample_period` from the fetch, so neither is typed at a call site. **Rule 7 could not be implemented the obvious way and the obstruction is worth keeping:** splitting `sample_period` into two fields turns the suite red, because `tests/test_governance_conformance.py` parses `Validation/Backtesting.md` and holds `Disclosure`'s field *names* to the document — so the rule changes the field's **type**, and the dates render into `content` as encoded ISO strings because `canonical_digest`'s own docstring asks callers not to lean on its `default=str` backstop. Cost was zero exactly as predicted; `git ls-files "*.yaml"` still returns nothing. Rule 4's `.gitignore` entry landed **before the first byte**, and the test asks `git check-ignore` itself rather than reading the file for a string, because a rule in the wrong section would read correctly and behave wrongly
- **The finding no Belay document had: the free equity source is survivor-only in its prices, and its universe table is not.** `SELECT date, act_symbol, close FROM ohlcv WHERE act_symbol='COH' AND date BETWEEN '2015-01-05' AND '2015-01-16'` returns **zero rows**, while AAPL over 2017-10-16..2017-10-26 returns three — Coach, Inc. traded actively until October 2017 and has no price history at all. Meanwhile `SELECT act_symbol, security_name, last_seen FROM symbol ORDER BY last_seen ASC` returns COH with `last_seen` 2017-10-26, in a table of **23,827 rows against roughly 6,000 currently-listed US stocks**, so dead tickers *accumulate* and membership is reconstructable as `last_seen >= date`. **The source fails the delisted-prices half and partly passes the delisted-universe half** — precisely the "a source can pass one and fail the other" case ADR-013 rule 5 sets out, arriving in the direction nobody predicted. Recorded as `SURVIVORS_ONLY` / `PARTIAL`, which under rule 9 is an *established* answer rather than an unknown one, and rule 5 bolts it to every artifact with its direction named
- **Two of ADR-013's Required Follow-Ups closed against the source rather than against a document.** DoltHub's page is JavaScript-rendered and yielded nothing, so both were answered through its SQL API. **Licence: CC BY-SA 4.0**, from the repository's own `LICENSE.md` via `SELECT doc_name, doc_text FROM dolt_docs`, whose Section 4 grants the right to "extract, reuse, reproduce, and Share all or a substantial portion of the contents of the database" — so **rule 4's storage ruling rests on unbounded repository growth alone rather than on a licence bar**. **Expired option contracts are retained**, established empirically: a contract observed 2019-02-09 expiring 2019-02-15 is still queryable in a database whose newest row is 2026-07-31, and a full chain for a 2019-03-15 expiry observed 2019-03-02 comes back across strikes and both rights. A live-chains-only source would have dropped these in 2019, so **options are backtestable at a zero budget**
- **A second adapter, to test whether the contract is a contract.** `framework/data/ibkr.py` required no change to `MarketDataSource` despite a structurally different wire format — epoch-millisecond timestamps and JSON floats against ISO dates and decimal strings, a duration-based request against a date-range one. **It has never run against the live IBKR API and says so inside its own survivorship basis**, so the admission travels onto every artifact rather than sitting in a comment nobody reads where the number is used. **IBKR was considered as the data source and does not solve the problem:** its own docs state "the API always requires Level 1 streaming real time data to return historical data", and "if data is not available for a specific instrument, data type, or period within a TWS chart it will also not be available from the API" — a delisted instrument has no chart. **It costs money and buys the same survivorship limitation**, and its fee waiver is tied to a monthly commission threshold a paper-first system generating no commissions cannot reach. **Using the broker as the data source would not violate rule 2** — that rule forbids a shared *abstraction*, not a shared vendor, and `test_the_contract_has_no_execution_vocabulary` asserts `MarketDataSource` never acquires one
- **The distinction that decides when to buy data, which the ADR stated the limit for but not the trigger.** **Survivorship bias is not "this data is wrong"; it is "selecting from this data is wrong".** A backtest of an instrument named in advance is unbiased for that instrument, which survived. The bias enters when instruments are *chosen* by screening a survivor-only universe, because everything that failed was removed before anyone looked. The free source is therefore adequate for named-instrument work and inadequate the moment Belay screens — which rule 6 already defers to Stage 3. **The trigger to buy is the first time Belay selects instruments rather than being handed them**, and nothing before that justifies the cost
- Finding 14 — **the transport ceiling, a failure recorded rather than worked around.** The boundary works; the hosted SQL API cannot serve a backfill. Measured: 8 days 0.4s, 1 month 0.5s, 6 weeks 0.6s, 2 months 28.8s, **3 months `context deadline exceeded` on 3/3 attempts**, and a *novel* month (April 2024) failed 2/2 at 54.5s while the same month in weekly chunks cost **111 seconds**. Thirteen years is roughly 6.6 hours per symbol. The adapter now chunks on **calendar months** — boundaries derived from the requested window alone, so the concatenated payload and its content hash stay comparable across re-fetches, which a tunable chunk size would have broken. **Two readings of this behaviour were wrong before the third was right**, and both are recorded: a one-month window returning in 0.5s was taken as representative when it only holds for windows the server has already served, and an initial diagnosis blaming primary-key ordering collapsed when the identical query returned in 0.4s moments later. Dolt's intended path is `dolt clone`; not built, because swapping the transport was outside the sanctioned scope. **No rule of ADR-013 is affected**
- **The suite does not touch the network, deliberately, and that is why it did not catch the above.** Both adapters take an injected `Callable[[str], bytes]`; the ceiling was found by running a real fetch by hand. Recorded in `docs/HANDOFF.md` as the thing to re-check before believing any future transport change

- **ADR-013 ratified 2026-07-31, Accepted as corrected, no rule amended. Not implemented — deferred on session budget rather than on doubt, the state ADR-006 and ADR-008 have both occupied.** ROADMAP Stage 2 is unblocked and is the next build; `docs/HANDOFF.md` carries a `★ START HERE` section naming the smallest honest slice, the two facts to establish against the data source rather than from a document, and four things not to do
- **ADR-013, the data boundary.** What ROADMAP Stage 2 is blocked on, and **ten rules rather than the five `docs/EndState.md` predicted** — the five it missed were invisible without opening `Knowledge/Identifiers.md`, `framework/artifacts/enums.py` and `constitution/Evidence_Standards.md` together. **Three constitutional lines nobody had cited:** `Operational_Constraints.md:29` puts "Tradable universes must be discovered through evidence" in the *constitution*, where `EndState.md` had rested the survivorship argument on a departmental document; `:25-27` — "No asset class, security, or investment methodology possesses inherent preference" — decides the equities-versus-options question against an equities-only boundary, because such a contract gives equities preference *by construction*; and `:9-17` distinguishes Facts from Assumptions, Predictions and Opinions, which is the vocabulary the evidence hierarchy lacks. **The structural ruling is rule 3 and it was forced rather than chosen:** the eight identifier prefixes and the nine `ArtifactType` members have no name for market data, so a fetched series is not an Artifact — the *fetch record* is, carrying the series' content hash, with `deliverable = None`, which `framework/artifacts/artifact.py:29-36` already calls "a real state, not an unset field". **`Knowledge/Versioning.md` turns out to be load-bearing for storage** and had never been cited for it: vendors restate history, so a backtest run against a series nobody kept is not reproducible and its irreproducibility is silent — a differing re-fetch is a new version, not a correction. Rule 4 keeps the store out of git because rule 3's hash already covers integrity, so committing the bytes adds no guarantee while making repository growth unbounded and irreversible
- **Two verification passes on ADR-013, and the second is why the first cannot be trusted.** The author's pass found one defect: rule 2 cited `Immutable_Laws.md:45` as Law VII when the line is Law VI, so the draft named two laws identically and contradicted its own Rejected Alternatives. **The independent pass then found three more defects and six imprecisions across 71 distinct claims**, all corrected and marked inline. (1) **Rule 6 rested on a premise no file in the repository contained** — the owner answered that Belay trades equities and options, nobody wrote it down, and the ADR cited `docs/OwnerDecisions.md` for it; the fact existed in three files written in one commit, each pointing at the others. Fixed at the root: `docs/OwnerDecisions.md` Part 4 question 5 now records the answer. (2) **"Eight rules" was a miscount; there are ten**, and it had already reached `docs/ROADMAP.md`, `CHANGELOG.md` and a commit subject — the same shape as the `Knowledge/Schema.md` miscount that reached a test docstring. (3) **Rule 7's supporting bullet was carried near-verbatim from `docs/EndState.md` and re-attributed**, which is precisely what the ADR's Status block claimed it had not done. **And the boast was itself unaudited:** "twenty-two citations checked, one wrong" used a denominator reproducible under no counting rule, and was quoted here before anyone tested it — reporting a good error rate against a made-up denominator is the shape ADR-012 rule 10 refuses, and doing it in a document about provenance is worse than reporting none. Real figures: **71 claims, 11 wrong or questionable, 4 defect-grade, of which the author's pass caught 1.** The practice is what this vindicates, not the draft
- Finding 12 — **the evidence classes have no class for observed fact.** `Evidence_Standards.md`'s four levels each grade how well a *strategy* has been tested; a fetched price is none of them, and grading it C would assert a simulation that did not occur. ADR-013 rule 8 routes around it soundly — a fetch record carries no evidence records, so it carries no grade — but the routing works only while nothing needs to *weigh* an observation against a simulation, and `Evidence_Standards.md:39` says the hierarchy "may never be reversed", which presumes everything sits on it
- Finding 13 — **opened on a false premise, then rewritten.** It read "option data is unreachable at the stated budget", resting on the claim that free sources do not carry historical option chains. **That was asserted from general knowledge rather than checked** — the failure recorded under `Citation discipline` — flagged by ADR-013's independent pass as an unsourced external claim and then falsified by going and looking. `post-no-preference/options` on DoltHub is free, daily-updated and version-controlled, carrying bids, asks, IVs and greeks for ~2,098 option symbols from 2019, ~6 GB as CSV. **What remains open is narrower and is verification rather than purchase:** whether expired contracts are retained (an option expires by design, so a live-only source is not defective, merely unusable for a backtest) and the exact licence, on which ADR-013 rule 4's storage ruling depends. A free path exists for the harder half too — point-in-time index membership approximated from a tracking ETF's monthly holdings back to 2006, **survivorship-*reduced* rather than survivorship-free**, since the ETF commits only to holding "at least 90%" of assets in index securities. **That is the best outcome for the ADR rather than a problem for it:** rule 5 asks two questions, the real answer is "partially, with these named gaps", and rule 9 already makes a stated partial answer legitimate where a silent assumption is not
- `docs/OwnerDecisions.md` — the answers no reading of this repository could produce, recorded so a later ADR cites them instead of re-asking. A fourth category beside DECIDED, IMPLIED and OPEN: **ANSWERED** means a fact now exists, not that a rule now binds, and each answer is written as what it settles, what it does not settle, and what it opens. **Belay will never place orders in an account that is not its operator's**, which closes the largest regulatory question in `docs/EndState.md` and voids the entire multi-user column of its cost table. **Belay will never run on hardware its operator does not control** — which contradicts `docs/ROADMAP.md:219-240`'s halt-within-seconds requirement and is recorded as finding 11. **Stage 9 collapses to reconciliation**, conditional on one broker and one account, and needs no new mechanism because ADR-011 already governs an unstated convention like a tax rate. And **row 1's answer opens a row nobody had**: distributing an application that produces investment recommendations is a different question from managing money, and closing the first does not close the second
- **The repository is public, verified, and public on a false premise.** `docs/EndState.md` row 13 said this could not be determined from inside the repository because `gh` was unavailable. It still is, so the check ran against the API — `curl -s -o /dev/null -w "%{http_code}\n" https://api.github.com/repos/pewpewpressco-ux/Atlas` returns **`200`** with no credentials presented, where a private repository returns `404`. It was made public in the belief that Claude Code required it for access; it does not — this repository is read from local disk and the remote is never fetched for content. **Nothing of substance is exposed** — 185 tracked files, no credentials, no market data, no account identifiers — so what was at risk was every future commit, which is why the fix is cheap now and not later. **Fixed the same day: the repository is private, verified by the same query returning `404`**, and the commits recording this session were held unpushed until that check passed. **Row 13's recommended answer changes with it** — while the repository was public, committing a fetched price series *was* redistribution and no free or retail licence tolerates that; private storage is not, so ADR-013 can weigh Law VII's retention obligation against `Knowledge/Versioning.md` on the merits rather than having the answer forced by a fact nobody had checked. It stays OPEN, and stops being blocked
- **The four operating questions answered, which is the shape of Belay and existed in no document before today: Belay runs on a small always-on machine in the owner's house; it trades a US account, taxable or tax-advantaged or both; it holds positions for days to months, deciding once a day on daily bars; and it starts on free market data, carrying the survivorship bias as a disclosure inside every artifact's signature rather than pretending to have corrected it.** Three consequences that change work: the always-on machine resolves finding 11 as first written and leaves a sharper residual, since Stage 8 tolerates a missed hour and an emergency brake does not; **"or both" undoes row 6's one-account condition written the same day** — the tax treatment becomes per-account, so an artifact must record which account it describes or two different numbers for one strategy are indistinguishable, the same defect ADR-011 refused for the risk-free rate; and a zero data budget makes the survivorship disclosure mandatory rather than conditional, because **a contract that cannot express "this source does not know past universe membership" cannot record the answer on the day a better source does** — which is the argument for ruling the boundary before writing the fetcher, stated in something other than principle
- Finding 11 — **a home machine cannot guarantee a halt within seconds.** The first conflict this repository has recorded between two *requirements* rather than between a document and its code. **Opened and then narrowed within the same session, and recorded that way rather than tidied into its final form**, because the sequence is the useful part: as first written it said own-hardware-only and halt-within-seconds cannot both hold, which was true while "your devices" could mean "my laptop, when I am using it"; the owner then answered that Belay gets a dedicated always-on machine, which resolves it. **What remains is sharper — a home machine is not a datacenter.** A power cut, an ISP outage or an OS rebooting at 3am each leave a position unattended without warning, so "within seconds" is met in the normal case and not met in exactly the cases that make anyone want it. The recommendation is a protective order resting at the broker as the primary halt with the machine as its supervisor, because **any halt depending on Belay being awake, connected and correct fails exactly when things are worst** — carrying the ADR-012 rule 7 caveat that a resting stop is not filled at the stop price in a gap. Recorded now rather than at Stage 7 because `docs/ROADMAP.md:219-221` already forbids discovering it late
- `docs/EndState.md` row 13 and `docs/ROADMAP.md` Stage 9 corrected in the ADR-007 manner — superseded wording preserved rather than replaced

Fixed:

- The test count in `docs/HANDOFF.md` and `docs/ROADMAP.md` read **387 and was stale by one** before this session began. Caught by noticing the figure move with no code written, and **confirmed rather than assumed**: the documents were stashed and the suite re-run against the unmodified tree, which also returned 388. The same drift as the "46 passing" figure the roadmap already records, at a smaller size

- `docs/EndState.md` — what Belay is for, what it must eventually do, and which decisions between here and there have actually been made. Fifteen decisions, each marked DECIDED with a `file:line`, IMPLIED with its reasoning and an explicit note that nothing rules it, or OPEN. **It rules nothing.** Three findings it records that finding 10 did not: `constitution/Mission.md:7` commits Belay to "purchasing power", which is an **after-tax** quantity, so tax-aware evaluation is implied by one adjective in the Mission; `constitution/README.md:5` already binds an "**AI process**" to the Constitution, so a runtime model is contemplated rather than prohibited, and `constitution/Evidence_Standards.md:31-35` already defines the class its output belongs to (Level D, "Research only"); and `AGENTS.md` — routinely cited as though binding — sits in **neither** of ADR-002's two layers, so "explicit interfaces" cannot carry the broker-adapter ruling. **The blocker on ROADMAP Stage 2 is the adapter question, not the who-is-Belay-for question:** Stage 2's deliverable is a *boundary*, and a boundary is an adapter contract. Survivorship bias returns **zero hits** across the whole repository including code, and is a Stage 2 acceptance criterion in two halves — delisted *prices* and delisted *universe membership* — of which a source can pass one and fail the other
- `tests/test_governance_conformance.py` — executable checks that the code matches the documents governing it. Passing checks assert a conformance that holds; `xfail(strict=True)` checks hold an open gap in the test suite rather than in prose, and turn the suite red when closed. The number still watched is deliberately not written here — it has changed in most sessions since, and `scripts/status.py` re-derives it on every run
- `StrategyLifecycle` — the eight capital stages, separated from `ArtifactLifecycle` per ADR-003. `Artifact` gained an optional `strategy_stage`
- `ArtifactRepository.versions()` and version-aware paths; eight persistence tests for code that had never been executed
- `framework/identifiers.py` gained the two missing prefixes (REGIME, DOC), format validation, and tests
- `docs/DocumentStandard.md` — was `NEW STANDARD`, an extensionless file at the repository root that no session had ever read
- `docs/notes/` — 18 original design notes, renamed from extensionless files so they are searchable
- `framework/artifacts/primitives.py` — `canonical_digest` and `utc_now`, the single hashing and clock primitives, importing nothing from Belay
- `ArtifactIntegrity.sign()` — the named signing operation
- `EvidenceRecord.canonical_payload()` and `EvidenceRecord.verify()` — a forged evidence hash was previously undetectable
- `docs/ROADMAP.md` — engineering sequencing, keyed to the seven promotion criteria in `constitution/Promotion_Pipeline.md`; defines no policy
- `scripts/status.py` — status dashboard re-derived from the repository on every run; reports untested and unimportable modules as prominently as passing tests
- `refresh-status.cmd` — double-click launcher; regenerates the dashboard and opens it in a browser
- `pyproject.toml` — project metadata, dependencies, explicit package list, pytest configuration
- Package `__init__.py` files across `framework/` and `departments/`
- ADR-004: demotion may move any distance downward, may not land below `PAPER_TRADING`, is always a review outcome rather than an automatic one, and requires written justification rather than a numeric threshold. Unblocks the only open P1; the ten resulting transitions are not yet in code
- ADR-004 amended before implementation: two tests break rather than one, and the second hangs instead of failing; `RETIRED` is the highest ordinal in `StrategyLifecycle`, so the prescribed "compare stage ordering" fix scores every retirement as a promotion unless retirement is classified first; rule 4's claim that written justification "needs nothing further" was withdrawn, having been asserted without checking `constitution/Capital_Authority.md`, which names six mandatory inputs and outranks the ADR under Immutable Law X
- ADR-004 rule 4 amended and ratified: stage and allocation are different decisions and a demotion is the first of two. The demotion is governed by rule 4 — written justification, no numeric gate — and triggers a second act, an allocation determination under `Capital_Authority.md` that sizes the strategy inside its new lower ceiling. The six inputs may never gate the demotion itself, because the same document holds that capital preservation wins whenever conflict exists
- Three findings recorded, two since closed: `ArtifactRepository.versions()` sorted versions lexicographically so "latest" broke past nine (closed by `version_key` and `latest()`); every `xfail` reason cited the retired 23-defect numbering rather than the current finding table (closed — markers cite section titles, which do not renumber); "Remain" is a documented review outcome that `validate_transition` rejects as a self-transition (ruled by ADR-008, blocked on the review-decision record)
- ADR-002: two-layer architecture, governance and mechanics, joined by the Artifact contract
- `framework/metrics/drawdown.py` — maximum drawdown; first component to emit an Artifact carrying a computed value
- ADR-004 implemented: ten backward transitions, no forward move changed. Both affected tests were rewritten *before* the table, because each used "everything except `RETIRED`" as a proxy for "forward" and one would have hung rather than failed once demotions shared the table
- `ArtifactRepository.latest()` — the one way to ask which version of an artifact is current. `versions()` was the only ordering the repository offered, so anything needing the current version had to index into it and get the ordering right itself
- `framework/artifacts/primitives.py` gained `version_key` and `is_semantic_version`; `ArtifactRepository.save()` now refuses a version that is not `MAJOR.MINOR.PATCH`, because append-only storage can never correct such a filename afterwards
- `RelationshipType` — the eleven types of `Knowledge/Relationships.md`, spelled as the document spells them. `ArtifactValidator` refuses anything else, and `Relationship` is now frozen like every other model in `framework/`
- `Artifact` gained `tags`, `summary`, `confidence` and a derived `evidence_level`, completing the required fields of `Knowledge/Schema.md`. All four are inside the integrity hash (ADR-005)
- `strongest_evidence_level` and `EVIDENCE_STRENGTH` in `framework/artifacts/enums.py` — an artifact's evidence grade is the strongest record it carries, computed by the factory and re-derived by the validator. The strength table is explicit rather than read from declaration order
- ADR-005: artifact-level confidence is revised by producing a new version, never in place; `Evidence Level` is derived and not settable
- ADR-006: the seven criteria of `constitution/Promotion_Pipeline.md` govern promotion. The five documented lists are not five rivals — they sit at three altitudes, and the 0-100 Promotion Score is suspended rather than adopted
- ADR-007: the doctrine's eight stages are the lifecycle; `Promotion_Pipeline.md`'s seven levels are a maturity model on a different axis
- ADR-008: `Remain` is a review outcome, not a lifecycle transition. No self-transition is added to the table
- `docs/Architecture.md` records the two-layer architecture, completing ADR-002's fourth required follow-up
- `tests/artifacts/test_relationships.py` and `tests/artifacts/test_confidence_and_evidence_level.py`
- Two tests asserting every row of the HANDOFF findings table matches its section heading. `scripts/status.py` counts sections while the table is maintained by hand, so a row could drift from its heading without moving any figure on the dashboard. Added after that drift occurred twice in one session; it immediately caught a third, older instance
- ADR-009, ratified and implemented: the review decision record is an Artifact of type `REVIEW`, carrying its subject in typed content, with justification mandatory for every outcome and the recorded outcome required to agree with the recorded stage change. Ratified as drafted — the first ADR here to need no amendment on ratification
- `framework/artifacts/review.py` — `review_artifact()` records a review decision as a signed Artifact and `review_fields()` reads one back. **Belay can now record that a review happened.** Three ratified obligations are discharged: ADR-004 rule 4 (written justification for a demotion, unenforced anywhere since the day it was ratified), ADR-004's re-sizing step, and ADR-008's `Remain Current Stage` — so a strategy held at a stage is no longer indistinguishable from one nobody looked at
- `ReviewOutcome` and `ConfidenceAdjustment` in `framework/artifacts/enums.py`. Four outcomes, not five: `Reject` resolves to `Remain Current Stage` per ADR-008 rule 4, because one event with two names must not become two members
- `STRATEGY_LADDER` in `framework/artifacts/enums.py` — the eight doctrine stages with `RETIRED` removed. ADR-004 warned its ordinal trap "becomes live the moment this helper is written"; ADR-009 rule 5 is that moment, and retirement is classified before any ladder position is taken. Two tests pin it: a retirement recorded as a promotion, and one recorded as a demotion, are both refused
- `ArtifactSerializer.load()` — **artifacts can be read back for the first time.** The acceptance test is the round trip through the signature rather than through field equality, because a loader that rebuilds every field but hashes differently has silently broken every signature it touched. Loading does not re-sign, so a tampered file loads and then fails the gate. Pinned against `dataclasses.fields(Artifact)` so a future field cannot be dropped in silence
- `test_strategy_lifecycle_is_the_doctrine_sequence_in_order` — parses the doctrine's eight stages and compares them to `StrategyLifecycle` as a **sequence**. The existing test checked only that each stage name appeared somewhere in the file, which a reordered doctrine would still have passed. ADR-007 follow-up
- `test_promotion_criteria_match_the_constitution` — parses `constitution/Promotion_Pipeline.md:75-81` and holds `scripts/status.py`'s `PROMOTION_CRITERIA` to it, wording included. ADR-006 follow-up. It earned its place immediately: it caught a `---` rule added to that document later the same session being parsed as an eighth, empty criterion, and the parser was tightened rather than the test loosened
- `ArtifactRepository.get()` — **the repository can return an artifact, not just a version number.** `latest()` handed back a version *string* because until `ArtifactSerializer.load()` landed there was nothing else it honestly could. With no version named, `get()` asks `latest()` which one is current, so it inherits that method's two conventions rather than deciding again: None when nothing is stored, a raise when nothing stored is orderable
- **`get()` verifies integrity on read, and the reasoning is in the method.** `save()` refuses unsigned and tampered artifacts, but a write-time check says nothing about the file afterwards — a YAML edited by hand on disk is invisible to it, and the read is the only place left that can notice. `ArtifactSerializer.load()` still loads such a file unchanged and unverified, which is correct for a parser: it should return what is on disk so the tampering is visible. `get()` is the repository vouching for a record, and its likeliest caller is a promotion gate asking for a strategy's current stage. This does not contradict "loading is not signing" — it depends on it, because a `load()` that re-signed would leave nothing here to check
- Two conformance tests over `strategies/Lifecycle.md`: one holding its annotation to its own sequence — anything it lists that is not a `STRATEGY_LADDER` stage must be explained in the note, so the two cannot drift apart — and one asserting the two lines quoted by ADR-004 and by `framework/artifacts/validator.py` are still in the file
- ADR-011, ratified and implemented: **no metric convention the documents do not state is ever defaulted.** `Validation/Backtesting.md` names nine metrics and defines none of them, so Sharpe's risk-free rate, Sortino's target return and every period count are required arguments — and each is recorded inside the artifact's integrity hash beside the number it produced, because a Sharpe of 1.4 at a 0% rate and one at 4% are different claims that a figure alone cannot distinguish. Dispersion is the sample estimator (`n-1`) and downside deviation divides by the full sample rather than by the count of shortfalls; both are the choice that cannot flatter a strategy, and both are invisible in the result, which is why they are ruled rather than left in a function body
- `framework/metrics/returns.py` (simple period returns, geometric CAGR), `framework/metrics/volatility.py` (sample standard deviation, annualization, downside deviation) and `framework/metrics/risk_adjusted.py` (Sharpe, Sortino). **`Risk-adjusted returns` — one of the seven constitutional promotion criteria — became computable**, moving `scripts/status.py` from 1 of 7 to 2 of 7. The path is not cosmetic: `scripts/status.py:50` names exactly that file
- `framework/metrics/reporting.py` — `Disclosure` and `metric_artifact`. **`Validation/Backtesting.md`'s requirement that a backtest document its Assumptions, Data Source, Sample Period and Known Limitations is executable for the first time.** `Disclosure` refuses a blank or whitespace-only entry, all four sit inside the integrity hash, and a conformance test parses the document so a fifth requirement added there turns the suite red. `EvidenceRecord` was deliberately **not** widened to hold them: its canonical payload is pinned by a literal digest, and adding fields would move every evidence hash ever computed
- ADR-012, ratified and implemented: **Belay can ask whether a track record is distinguishable from luck.** Seven documents demand statistical validity and none defines it — `constitution/Promotion_Pipeline.md:27` defines maturity Level 2 *as* "Passed statistical review", the only one of the seven levels defined by passing a test, and `constitution/Capital_Authority.md:11-14` makes `Statistical edge` one of six inputs that determine capital. The null hypothesis and alpha are required arguments; the test is two-sided, because a one-sided test assumes the sign of the effect under examination and halves the p-value for free
- `framework/metrics/distributions.py` — Student's t, **implemented rather than imported**. `scipy` is neither installed nor declared, and Immutable Law II prohibits black-box reasoning: thirty-five lines pinned against fifteen values from a *published* t-table are explainable in the sense that law means, where "the library said so" is not. Testing one implementation against another proves they agree; testing against a printed table proves this one is right. Worst deviation 7.5e-05, inside the table's own rounding. A leaf module importing nothing from Belay, the role `framework/artifacts/primitives.py` plays on the artifact side. `numpy` was rejected too — present only as a transitive dependency of `pandas` and declared nowhere
- `framework/metrics/statistics.py` — standard error, t-statistic, two-sided p-value, confidence interval, and how large a sample an observed effect would need. **`Statistical performance`, the first of the seven constitutional criteria, became computable**; dashboard 2 of 7 → 3 of 7. Two rules carry the weight. Rule 7: the t-test assumes i.i.d. returns, real returns are not, and the consequence has a *direction* — the p-value is **optimistic**, understating the chance a record is luck. `ASSUMPTIONS` and `KNOWN_LIMITATIONS` are fixed constants with no parameter to override them, so the caveat cannot be softened. Rule 8: "not significant" and "not enough data to tell" are different states, and `minimum_observations_for_significance` answers the second — a sample below it has not failed the test, it has not taken it
- `docs/ROADMAP.md` Stages 6 to 9 — execution and broker integration, live position state and the halt mechanism, the daily operating loop, and tax lots. **The roadmap previously ended at Stage 5 and did not reach the mission.** `constitution/Mission.md:5` states Belay exists for "evidence-driven capital allocation" and line 38 that it "optimizes for decades—not days", yet no stage deployed capital, placed an order, held a position or reported a tax lot. The omission was invisible because every stage that *was* listed is real and well-sequenced. Found by the repository owner asking how funds, execution and taxes would eventually work. The four new stages are **named so the gap is visible and explicitly not scoped** — each records which ruling it needs first, and Stage 9's very existence is a scope question, since `tax`, `wash sale`, `cost basis` and `1099` return zero hits across all 101 documents. Dashboard: 1 of 6 stages complete → 1 of 10
- Finding: **The Roadmap Does Not Reach The Mission** (P2). Its checkable edge is that `framework/services/execution.py` and `framework/services/liquidity.py` are expected by `scripts/status.py` for two constitutional promotion criteria, exist as no file, and appeared in no plan. Findings 2 (empty workflow registry) and 8 (no halt mechanism) turn out to be symptoms of the same absence — and `workflows/` already documents thirteen workflows including `Daily/MarketOpen`, `MiddayReview` and `MarketClose`, so the operating layer was specified all along and merely unsequenced
- `test_every_expected_component_is_either_built_or_planned` — **written as a strict `xfail` and had its marker removed inside the same commit**, because the roadmap amendment closed the exact condition it watched and the suite went red with `XPASS(strict)` on the next run. The clearest demonstration in this repository of that mechanism working, and it caught its own author. It is now a passing regression guard: deleting Stages 6 to 9, or dropping a criterion's component from the plan, turns the suite red. It deliberately does not track whether any of it is *built* — the dashboard already counts that, and one gap with two counters that can disagree is a defect recorded here twice already
- Rule 11 deliberately **did not** write the "Passed statistical review" gate. `significant` is `p < alpha` and nothing more; the promotion decision remains a recorded review, the line ADR-009 rule 5 drew. A test asserts the module exposes no name containing `passes`, `promote`, `approve`, `verdict`, `gate` or `decision`

Changed:

- Three departmental documents reconciled to the constitutional wording (ADR-006 rules 3 and 5). `workflows/Monthly/StrategyPromotion.md`: `Risk` → `Risk-adjusted returns`, `Operational Stability` → `Operational consistency`, `Regime Compatibility` → `Regime robustness`, and `Statistical performance`, `Execution quality` and `Liquidity` added — the three omissions ADR-006 rule 5 names. `Validation/CapitalReview.md`: `Operational readiness` → `Operational consistency`. `strategies/PromotionCriteria.md`: `Operational Stability` → `Operational consistency`, `Risk` → `Risk-adjusted returns`. `Drawdown` was left alone in all three: ADR-006 treats it as the one criterion already named identically
- `constitution/Promotion_Pipeline.md` records that its seven levels are a maturity model rather than the lifecycle (ADR-007 rule 2), and that its criteria list is the authoritative one (ADR-006 rule 1). Its first line already said "maturity level"; what was missing was the distinction from the doctrine's eight stages, which is the confusion that put a false citation inside Accepted ADR-002
- `strategies/PromotionCriteria.md` records that its 0-100 Promotion Score is suspended, not adopted (ADR-006 rule 4). The document previously read as though the score were live
- `strategies/ReviewTemplate.md:51` reads `Remain Current Stage` rather than bare `Remain`, and `Validation/CapitalReview.md` now defines `Reject` — the two ADR-008 follow-ups that were blocked until the review-decision layer existed
- `DeliverableType` and `DELIVERABLE_ARTIFACT_TYPE` in `framework/artifacts/enums.py`, and `Artifact.deliverable` — **ADR-010 implemented, and the longest-open P2 finding closes with it.** Twenty-four members spelled as the eleven declaring documents spell them, plurals included, held to those documents by a conformance test so a deliverable added to a departmental document without a member turns the suite red. Inside the integrity hash (rule 8, the reasoning ADR-005 gave for `confidence`). `ArtifactValidator` refuses an unknown subtype — the check that makes this shape safe, since a free string would have made `artifact_type="UniverseReport"` legal rather than fixing it — and refuses a pair that contradicts itself, because rule 9 makes the pair meaningful. The deliverable-to-type mapping was the part ADR-010 deferred to implementation: the principle is that the type describes what the artifact *is* rather than who produced it, `REPORT` covers thirteen of the twenty-four, and the four genuinely arguable calls are named in the code rather than buried
- `framework/metrics/drawdown.py` migrated onto `metric_artifact` (ADR-011 rule 13). Its `source=` string is **gone rather than deprecated** — nothing is stored, so nothing built the old way exists to break, and a second accepted signature would leave the pre-ADR-011 pattern legal. `docs/HANDOFF.md` calls that module "the pattern to follow", and leaving the one worked example as the one component not following the rule would have made the rule optional on the day it was ratified
- `metric_artifact` gained a `deliverable` parameter defaulting to `Backtest Reports`, and now derives the artifact *type* from `DELIVERABLE_ARTIFACT_TYPE` rather than hardcoding it, so the pair cannot contradict itself. ADR-012 rule 12 needed `Validation Reports`, because `Validation/Workflow.md:15` makes Statistical Review a step distinct from Backtesting
- `METRIC_TOKENS["Tail Events"]` tightened from `tail` to `tail_event` (ADR-012 rule 13). **The looser token had a false positive, found the day after that watcher was written and by the very next piece of work**: a two-sided significance test is naturally named `two_tailed_p_value`, which would have made the watcher report `Tail Events` as *built* while it was untouched. A watcher that reports a gap closed when it is open is worse than no watcher
- `test_artifact_type_can_name_the_documented_deliverables` **has its `xfail` marker removed** — the last of the six weak watchers, and the only one that could not be strengthened when the others were, because what it should assert *was* the open question. It now parses the eleven documents and asserts every declared deliverable resolves to a pair. All six watchers are honest for the first time
- `test_artifact_types_and_identifier_prefixes_name_the_same_kinds` — a new `xfail(strict=True)` watcher over a new finding, found by implementing rather than by reading. ADR-010 rule 9's mapping meant comparing `ArtifactType` against `Knowledge/Identifiers.md` for the first time: `PORTFOLIO` and `FAILURE` are members with no prefix, `DOC` is a prefix with no member. Not cosmetic — three deliverables map to `PORTFOLIO` or `FAILURE`, and `ArtifactFactory` refuses `PORTFOLIO-0001`, so those artifacts cannot receive an identifier naming what they are. Demonstrated against the running code. Closing it is a ruling either way, so nothing here guesses which
- **ADR-010 accepted and implemented** (proposed and ratified the same day, as drafted): `ArtifactType` is a coarse type with a constrained subtype. The finding it answers had been open since 2026-07-26 and routed around three times, resting on an estimate — "roughly twenty deliverables" — nobody had counted. All 31 documents under `departments/`, `Research/`, `Validation/`, `Operations/` and `workflows/` were read: eleven declare deliverables under an explicit heading, 34 entries, **25 distinct**, plus four named only in prose. 29 against nine members, so the estimate was low. Three things the estimate hid decide the ruling: the enum can already *type* almost all of them but cannot *distinguish* them (sixteen of 25 are honestly `REPORT` today); seven are declared by more than one document, `Universe Report` three times across a department, a function and a daily workflow; and three entries — `Knowledge Update`, `Repository Update`, `Knowledge Graph Update` — are effects in `WorkflowEngine.md`'s execution model rather than artifacts. The shape is not invented: ADR-009 already chose coarse-type-plus-typed-detail for review records and its Rejected Alternatives refused to add a member for exactly this finding's reason. The subtype vocabulary is parsed against the eleven documents rather than transcribed, so no new governance document is created and ADR-002's freeze is untouched. **Proposed, not ratified; `ArtifactType` is unchanged and nothing is implemented**
- `test_every_workflow_document_defines_what_the_readme_requires` — a new `xfail(strict=True)` watcher, and the finding it watches is materially wider than it was recorded as being. `Workflow Base Class Incomplete; Registry Empty` was stated as a code defect: `base.py` provides two of the five things `workflows/README.md` requires. All thirteen workflow documents were then checked rather than the two that happened to be open, and **not one of them defines all five either** — the best is two, and `Monthly/StrategyPromotion.md` and `Weekly/WeeklyPortfolioReview.md` define none. So `Workflow` cannot be filled in by transcribing these documents; whoever implements it is writing doctrine and should expect to need a ruling
- A second event-constant conflation recorded, distinct from `MONTHLY_REVIEW`. Both `StrategyPromotion.md` documents were opened: `Events/` is triggered by "Promotion threshold satisfied" and runs one candidate through Validation → Operations → Knowledge → Investment Committee, while `Monthly/` is calendared, evaluates every candidate, and ends in a `ReviewOutcome`. Two workflows, not one file duplicated — but `Event.PROMOTION = "promotion-review"` is a single constant whose value matches the monthly document's title while living in the event vocabulary, so it names neither unambiguously
- **Three weak `xfail(strict=True)` watchers strengthened**, each now parsing its governing document instead of a list transcribed from it. `test_workflow_defines_everything_the_documentation_requires` asserted `hasattr` for five names, two of which already existed as empty placeholders — it now requires each of the five things `workflows/README.md` names to be *abstract* on `Workflow`, the mechanism the class already uses for `execute`. `test_every_documented_workflow_is_registered` asserted `len(WORKFLOWS) >= documented`, which thirteen unrelated entries satisfied, and derived `documented` by subtracting 2 from a glob — it now names the two non-workflow files, requires an exact count, and checks each documented workflow is findable among the keys. `test_every_document_carries_the_metadata_block` asserted `startswith("---")` and nothing about contents — it now parses the twelve keys of the example in `docs/DocumentStandard.md` and checks every block against them. **All three still `xfail`, verified under `--runxfail` to be failing for the intended reason rather than on a parser bug** — an xfail hides the assertion, so a broken parser would look identical to an open gap. Five of the six audited watchers are now honest; the `ArtifactType` one stays weak on purpose, because what it should assert depends on a ruling nobody has made
- `docs/DocumentStandard.md` and `docs/HANDOFF.md` no longer state how many documents exist. Written as 81 and 82 respectively, the real figure was 100 on 2026-07-31; both are now derived by the watcher, the same fix applied to the `Knowledge/Schema.md` field count that had reached a docstring while wrong
- `strategies/Lifecycle.md` records that retirement is not the ninth rung of its ladder (ADR-007 rule 3), closing the last thing that finding was open on. Its nine `↓`-joined entries presented `Production → Retirement` as the move a strategy makes by surviving Production, when the doctrine places retirement outside the sequence and reachable from anywhere. The note also names the three further divergences from the doctrine — it omits `Idea` and opens at `Research`, inserts `Hypothesis`, and writes `Paper` for `Paper Trading` — and records the `RETIRED` ordinal trap, because this document is what makes that mistake look reasonable. Annotated, not rewritten: lines 39 and 41 are quoted by ADR-004 and by `framework/artifacts/validator.py`, and every original line survives
- `ArtifactSerializer.dump` renders timestamps as `isoformat` strings rather than native YAML timestamps. The comment first justifying this claimed PyYAML returns offset timestamps naive; **checked directly, the installed version round-trips `+05:00` exactly**, so no live bug was being fixed and the reasoning was corrected in place. Kept on the honest ground: `canonical_payload` hashes `created.isoformat()`, which makes the rendering signature-critical, while PyYAML's timestamp handling is library behaviour Belay neither controls nor pins — and older releases did return naive UTC. A stored timestamp with no offset is refused rather than assumed

Fixed:

- `ArtifactLifecycle` was missing `MICRO_CAPITAL`, the eighth stage of `constitution/Paper_First_Capital_Doctrine.md`, so the lifecycle stepped from promotion review straight to limited capital. A strategy's first exposure to real money would also have been its largest. `PROMOTION_REVIEW → LIMITED_CAPITAL` is now rejected
- An artifact's author and creation date could be rewritten while verification still returned `True` — neither was covered by the integrity hash, nor was `parent_hash`. The signature proved what an artifact said, not who produced it or when (P1)
- `ArtifactRepository.save()` wrote to `{id}.yaml`, so a second version silently destroyed the first — forbidden by ten documents. Artifacts are now stored per version and never overwritten (P1)
- `ArtifactLifecycle` carried the name of `Knowledge/ArtifactLifecycle.md` and the contents of the strategy capital sequence, so a drawdown report's legal future states included Micro Capital. Split per ADR-003 (P1)
- Retirement was reachable only from `PRODUCTION`, though the doctrine permits it at any stage (P2)
- Identifier format was unenforced and two of the eight documented prefixes were missing, so `DD-0001` — a prefix that never existed — was in use (P2)
- `confidence` accepted any float, including 42.0 and -3.0, against a constitution stating confidence is probabilistic (P2)
- Runtime dependencies were declared but not installed, and eight `framework/services/` placeholders raised `NameError` on import. Modules that will not import: 15 → 0 (P2)
- Nineteen files were tracked without extensions, invisible to searches and to GitHub. One was a repository-wide specification no session had read (P2)
- Unsigned artifacts passed validation. `ArtifactValidator` now rejects them, and `ArtifactFactory` signs everything it emits (P1)
- `EvidenceRecord` and `ArtifactIntegrity` ran separate SHA256 implementations that had already diverged. Both now use one primitive; no existing hash changed (P1)
- `datetime.utcnow()` replaced throughout. Naive timestamps are rejected by `EvidenceRecord`, which is the only place a timestamp enters a hash. 16 deprecation warnings eliminated (P2)
- `AGENTS.md` told every session to read three files that were misnamed or absent, so the mandated workflow failed at step one. All six paths now resolve (P2)
- Case-collision between `Knowledge/` and `knowledge/`, which merged silently on case-insensitive filesystems and risked data loss
- A bare `pytest` failed with four collection errors; both `pytest` and `python -m pytest` now work

Removed:

- `framework/requirements.txt`, superseded by `pyproject.toml`

Notes:

- Test suite executed for the first time: 28 passing, now 166 passing with 4 watched gaps
- ADR-003 accepted: one lifecycle per governed subject. A third, for engineering changes, is specified in `docs/ENGINEERING_AGENT.md` and should follow the same pattern rather than reuse either enum
- Nine of the 22 recorded defects closed; the eight checkable remainder are held as `xfail(strict=True)` tests. Five need a governance decision or are housekeeping
- ADR-003 proposed (not accepted): the `ArtifactLifecycle` enum carries the name of `Knowledge/ArtifactLifecycle.md` and the contents of the strategy capital sequence. Only "Draft" appears in both
- Every document in the repository was read against the code for the first time — 11 constitution documents, 15 under `Knowledge/`, 14 departmental, 13 workflows, 12 under `strategies/`, four under `docs/`, the root README and 19 extensionless design notes. 23 defects recorded in `docs/HANDOFF.md`, four of them P1
- `docs/ROADMAP.md` gained a "specified but not built" inventory — 15 specifications with no implementation — so the same gap is not rediscovered each session
- Demotion raised to P1: five documents require it and three treat it as a formal review outcome, but every transition in the table moves forward, so a "Demote" decision cannot be recorded
- ADR-002's "eight promotion gates" phrase was not merely unattributed but false, and has been amended by ADR-007 to cite `constitution/Paper_First_Capital_Doctrine.md`. `Promotion_Pipeline.md` contains no group of eight — it has seven maturity levels and seven promotion considerations. The original sentence is preserved in the amendment note rather than discarded
- Both P1 findings in `docs/HANDOFF.md` raised from observed to confirmed, then closed
- Artifacts are signed at creation. `Artifact` does not sign itself: constructor-time signing would hand every `dataclasses.replace()` copy a fresh valid signature and silently disable tamper detection
- The green suite conceals scope. It began at 28 framework modules referenced by no test and 15 that could not be imported at all; both figures are re-derived on every run by `scripts/status.py` and reported alongside the passing count. Now 23 untested and 0 unimportable
- Three of this repository's `xfail(strict=True)` watching tests were weaker than their names and would have gone green without their finding being closed. `test_confidence_can_be_adjusted` asserted only that a `confidence` field existed, so closing a *different* finding would have flipped it; `test_relationship_types_are_constrained` asserted only that a name existed, which an empty enum satisfies. Both were strengthened before being satisfied. The remaining four have not been audited for the same defect
- `Knowledge/Schema.md` lists thirteen required fields, not fourteen. The wrong figure had propagated from `docs/HANDOFF.md` into a test docstring, and a reading agent repeated it rather than counting. Counted by hand: lines 7-31, with `Optional Fields` beginning at line 35
- Eleven commits across two sessions sat unpushed while the work was reported closed out. `origin/main` was three days stale. Pushing is now part of session close-out

---

## v1.0.0-alpha

Repository initialized.

Added:

- Repository structure
- Constitution framework
- Department architecture
- Paper-first philosophy
- Promotion lifecycle
