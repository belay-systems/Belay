# 2026-09-25 — Implementing the owner's rulings on F-033 and F-036 (Part 35)

Issue #35, branch `claude/brave-pascal-e7401x`, continuing from
`docs/sessions/2026-09-25-review-falsification.md`. A second file rather than an
edit to that one: it had reached the 300-line cap, and `AGENTS.md`'s "one record
per file, for anything that only grows" is better served by a new dated record
than by raising the cap. Written once; never edited after merge.

## What happened

The two questions that session put to the owner were answered. **Owner said
"Aligned to recommendations".** Recorded verbatim as `docs/OwnerDecisions.md`
Part 35, with 35a–35d saying what it settles and a "does not settle" list.

- **F-033.** A computed number is Level C only when its series came from a
  recorded fetch; Level D otherwise.
- **F-036.** `pandas`, `jinja2` and `python-dateutil` are removed from
  `pyproject.toml`. `numpy` is **not** declared in their place — the alternative
  was offered in the question and not taken.

## The recommendation could not be built as it was described

This is the part worth reading, and it is a defect in how the question was put
rather than in the ruling.

The F-033 recommendation told the owner that "`Disclosure` gains a constructor
split". Building it showed that `tests/test_governance_conformance.py` asserts set
*equality* between `dataclasses.fields(Disclosure)` and the four things
`Validation/Backtesting.md` requires a backtest to document:

```python
fields = {field.name for field in dataclasses.fields(Disclosure)}
assert {name.lower().replace(" ", "_") for name in documented} == fields
```

That document is frozen by ADR-002, so **a fifth field on `Disclosure` would turn
the constitution's own validation conformance red**, and the only ways round it are
to change a frozen document or weaken the test — neither of which an agent may do.

The owner therefore agreed to a mechanism that does not exist. The substance ruled
is unaffected, and the built mechanism delivers exactly it: `Disclosure` gains an
`evidence_level` **property** returning `RESEARCH`, a frozen subclass
`FetchedDisclosure` overrides it with `HISTORICAL`, and `disclosure_from` — the
only function that takes a source and an arrived series rather than strings —
returns the subclass. Verified that this keeps the four-field assertion intact:

```
fields(Disclosure): ['assumptions', 'data_source', 'sample_period', 'known_limitations']
hand-built  -> EvidenceLevel.RESEARCH
fetched     -> EvidenceLevel.HISTORICAL
```

No new field enters an artifact's `content`.

**A claim here was false and is withdrawn.** This paragraph originally added that
"every artifact's integrity hash is byte-identical to before". It is not.
`ArtifactIntegrity.canonical_payload` covers `artifact.evidence_level` and each
`evidence.hash`, and `EvidenceRecord.hash` covers `level` — the grade was put
inside the hash deliberately, by ADR-005. Same artifact, fixed timestamp, built
under each commit: `f31a486f…` (Level C) before, `b21db6ed…` (Level D) after. Only
`content` is unchanged. **Consequence:** an artifact built before this change over a
hand-built disclosure no longer reproduces its stored hash. Nothing in the
repository is affected — the one committed artifact is a fetch record — but a stored
metric artifact would have been invalidated silently, and the false sentence is what
would have stopped anyone looking. Found by the independent pass.

Recorded in Part 35 under "Session readings, not the owner's words", and in full in
`docs/proposals/ADR-017-grade-from-provenance-DRAFT.md`. **Same failure shape as
the two defects this session had withdrawn hours earlier**: a recommendation made
without opening the thing that constrained it.

## What changed

**First production code in this workstream.** Everything before it was tests and
documents.

- `framework/metrics/reporting.py` — `Disclosure.evidence_level` property;
  `FetchedDisclosure`; `metric_artifact` reads `disclosure.evidence_level` instead
  of hardcoding `EvidenceLevel.HISTORICAL`. That hardcoded line was F-033.
- `framework/data/fetch_record.py` — `disclosure_from` returns `FetchedDisclosure`.
- `pyproject.toml` — the three dependencies removed, with a comment recording the
  ruling and why `numpy` was not declared.
- `docs/proposals/ADR-017-grade-from-provenance-DRAFT.md` — **PROPOSED. Not
  ratified; only the owner ratifies an ADR.**

**Two existing tests changed, and both had encoded the defect.**
`test_the_evidence_is_graded_historical` asserted Level C on a fixture whose own
`data_source` reads "unit test fixture, supplied by hand";
`tests/metrics/test_drawdown.py` did the same. Both now assert Level D and say in
their docstrings what they used to claim. **These are the only two tests in 709
that the change breaks**, which is a fact about how little of the suite ever
exercised provenance.

**One test added**, in `tests/data/test_fetch_record.py`:
`test_a_derived_disclosure_grades_the_metric_historical_and_a_hand_built_one_does_not`.
It asserts the pair — derived is C, hand-built is D, and the two are identical in
every documented field. Built so that `disclosure_from` returning a plain
`Disclosure` fails it, because a guard asserted by nothing is F-019's and F-032's
shape and this session exists because of it.

## Verification, and what is missing from it

**F-036 is verified properly.** A fresh virtual environment, installed from the
edited `pyproject.toml`:

```
$ pip list | grep -iE 'pandas|numpy|jinja|dateutil'
(none present)
$ python -m pytest -q
709 passed, 1 skipped, 5 xfailed      # at the time; 710 at 9d91870 — see below
$ python scripts/status.py     # exit 0
```

That run is the reason to trust the removal: the suite passed with none of the four
packages installed, not merely with them unused. The independent pass went further
and found nothing: all six `scripts/` run, `reports/generated/status.html` renders
well-formed at 16,118 bytes, and a walk of every module under `framework/`,
`departments/` and `scripts/` gives four import failures that are **identical with
the packages installed** — all pre-existing.

**The count in that block is stale and the reason is worth keeping.** `709` was the
*passing* count of a run that was also red — 709 passed plus 1 failed. Fixing that
failure (`9d91870`) made it **710 passed**, so the number moved while every sentence
quoting it stayed still. It was wrong in both directions across two consecutive
commits. `AGENTS.md` asks for the exact command beside a count; the lesson is that a
count copied forward stops being evidence the moment the tree moves.

**F-033's suite run is incomplete, and this is stated rather than implied.** The
sandbox blocked `python -m pytest` partway through this work. What was verified
first-hand:

- The F-033 construction that produced the finding now grades `RESEARCH`
  (`validates: True`, `evidence_level: EvidenceLevel.RESEARCH`).
- A full run against the production change before the test edits: **exactly two
  failures**, both the tests named above, both asserting `HISTORICAL` on hand-built
  fixtures.
- `fields(Disclosure)` still four; the property/subclass behaviour as printed above.
- All five changed files compile.

**Not verified locally: the suite after the two test edits and the new test.** The
edits set the expected values to the two observed failures, so the expectation is
green, but an expectation is not a run. **Continuous integration on the pull
request is the first full verification of F-033**, which `AGENTS.md` point 3 makes
the gate in any case. The mutation the ADR requires — `disclosure_from` returning a
plain `Disclosure`, which must turn the suite red — **has not been run** and is
owed.

## The independent pass ran, and the headline is that this does not deliver 35a

Run at 16:35 UTC on #36: **two blocking, six defects, four notes**, re-checked
against `9d91870`. Every one verified here before acting. It also discharged the
verification this record said was missing — including the mutation ADR-017 demanded:
`disclosure_from` returning a plain `Disclosure` turns the suite red on **exactly
one** test, so the new guard is asserted. The vacuity check, the conformance claim,
the round-trip, and the whole of F-036 all survived fourteen attacks.

**What did not survive is the central claim.** `disclosure_from` takes a
`DailyBarSeries`, which any caller can construct. It touches no payload, no
`content_hash`, no stored version, no fetch record. So:

```
disclosure type:  FetchedDisclosure
evidence_level:   EvidenceLevel.HISTORICAL   <- Level C
data_source:      DoltHub post-no-preference/stocks (CC BY-SA 4.0)
validates:        True
```

Eight bars typed into a file, Belay's own source, no subclass and no network.
Reproduced here byte for byte. **It is worse than F-033 in one respect:** F-033's
construction printed `data_source: I made these up`, disclosing its own
worthlessness; this one carries a real vendor's name and licence, so the grade and
the source agree and both are wrong. It is the substitution the `source` parameter
was designed to prevent, arriving through the unguarded `series` door.

The code enforces "came through `disclosure_from`". **Part 35a rules "came from a
recorded fetch". The ruling is right and the code is short of it** — so the fix is
not to soften the ruling. It is put to the owner as one question
(`docs/OperatorChecklist.md`, 2026-09-25) because it decides what a metric artifact
must carry and touches ADR-014. Nothing was implemented either way.

The overclaiming has been removed from `framework/data/fetch_record.py`, ADR-017's
Decision, Part 35's "Session readings" and this record, so no reader takes the grade
for more than "fetch-shaped".

## What is open

- **The owner's answer on Level C**, above. **ADR-017 must not be ratified before
  it** — as written it would certify the weaker behaviour as delivering 35a.
- **ADR-017's ratification.** Owner's act, not done.
- **A test for `significance_artifact`'s Level D**, which nothing asserts today —
  the same shape as F-019 and F-032.
- **An independent pass over these corrections**, which were written after the pass
  that found them.
- **F-003.** Nothing calls `disclosure_from`, so every metric artifact is Level D
  today. The accepted cost (Part 35b), and the reason F-003 now blocks more than it
  did.
- **ADR-016's evidence floor** interacts with this: under its draft, all-Level-D
  metrics would block promotion above `Paper Trading`. Whoever finishes it should
  read Part 35b rather than discover it.

## Part 36 — three implementations, two broken by independent passes

Asked whether Level C should require the stored fetch record, the owner answered
**"yes require the stored fetch record"**. Recorded as Part 36. That is the outcome
the recommendation argued for and the opposite of the shortcut, which was to amend
Part 35a to fit weaker code.

**The ruling was right all three times. The implementation was wrong twice, in the
same direction each time.**

| attempt | enforced | broken by |
|---|---|---|
| 1 | came through `disclosure_from` | `DailyBarSeries` is caller-buildable — eight typed bars + a real source → Level C with that vendor's name |
| 2 | the caller's record validates | `Fetch` is caller-buildable and `ArtifactIntegrity.sign` is **public and unkeyed** → Level C claiming a licensed vendor, 25 years, "Survivorship-free", `MarketDataSource` never instantiated |
| 3 | the record is **in the repository** | standing |

**The lesson from attempt 2 is the one worth keeping.** Its whole argument was "a
forged disclosure needs a forged signature". Verified:
`grep -rniE "hmac|secret|private_key|signing_key" framework/artifacts/` returns
nothing, and `ArtifactIntegrity.sign` is a public classmethod. So
`ArtifactValidator` proves a record is *internally consistent*, never that
`fetch_record` wrote it. I used "signed" as though it meant authenticity through two
implementations and four documents.

**What attempt 3 does.** Nothing the caller hands in is trusted but the identifier:
the record is re-read from the repository, the bytes come from `store.root` plus the
record's **own signed `store_path`**, and the series is checked against the record by
count and by window.

**The guarantee, exactly:** this record is on disk in the repository where it says it
is, these are its bytes, this is its series. **Not** that the bytes came from the
vendor — nothing here can prove that. What it raises is the cost: faking provenance
now means writing a permanent, discoverable record into the repository.

**Keyed signing was sized on the owner's question and rejected for this purpose.**
86-line core, 16 call sites, **26 test files**, an ADR-005 amendment, and a head-on
collision with "the same inputs must give the same outputs, on any machine". And
decisively: the adversary is the caller *in this process*, which can reach a key as
easily as the code can. It would not have blocked a single forgery found today.

**All eight guards are mutation-asserted**, and two of my tests were vacuous when
written — the mutation caught them, not review. The path test wrote identical bytes
to its decoy; the count test used bars outside the record's window so the window
check fired first. Both survived their own removal at 719 passing.

**Documentation corrections, all in the flattering direction, all mine:**

- Part 36's 36a stated *"A `Fetch` cannot be had without `fetch_and_record` having
  run"* — false, and sitting in "What it settles" where it read as the ruling.
- Refusal-rather-than-downgrade was filed under "What it settles". The owner was
  never asked what happens on failure. Moved to "Session readings".
- The RPT-0001 justification for the shape check was **wrong, and testable**: that
  artifact's hash is over its own stored content, so changing the emitting code
  cannot touch it. The real reason a marker field is no better is that it is equally
  forgeable.
- ADR-017's Decision still described attempt 1's signature, forty lines above its own
  correct description.
- ADR-017 claimed it closed "every route that does not involve running
  `fetch_and_record`", and called one limit "the one honest limit" when there are
  three.

**A process mistake, recorded because it nearly cost the work.** A mutation harness
ran `git checkout --` on a file whose implementation was not committed and reverted
all of it; the next four probes reported nine identical failures that were the new
tests failing against the *old* function. The identical counts were the tell. Commit
before mutating. Done that way for attempt 3.

### Still open

- **ADR-017's ratification.** Owner's act. **No independent pass on attempt 3** —
  three have run and each broke what it was given.
- ~~A test for `significance_artifact`'s Level D~~ — **added**, and confirmed not
  vacuous by reinstating the hardcoded grade. Its absence was a claim in ADR-017
  resting on no test.
- **Authenticity**, permanently, absent keyed signing. Two narrower routes remain:
  building a `FetchedDisclosure` directly, or subclassing `Disclosure` to override
  `evidence_level` — the second is not greppable by class name, which the earlier
  draft's mitigation assumed.
- **F-003 unchanged.** Nothing calls `disclosure_from` in production, so every metric
  artifact is still Level D.
